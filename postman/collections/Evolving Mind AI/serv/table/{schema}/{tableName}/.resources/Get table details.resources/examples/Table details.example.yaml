$kind: http-example
request:
  url: http://{{baseUrl}}/serv/table/:schema/:tableName
  method: GET
  headers:
    Accept: application/json
  pathVariables:
    schema: string
    tableName: string
  body:
    type: text
    content: ""
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "name": "string",
        "schema": "string",
        "columns": [
          {
            "name": "string",
            "type": "string"
          },
          {
            "name": "string",
            "type": "string"
          }
        ]
      }
order: 1000
