$kind: http-example
request:
  url: http://{{baseUrl}}/serv/table
  method: POST
  headers:
    Content-Type: application/json
    Accept: application/json
  body:
    type: json
    content: |-
      {
        "name": "string",
        "schema": "string",
        "columns": [
          {
            "name": "string",
            "type": "string"
          },
          {
            "name": "string",
            "type": "string"
          }
        ]
      }
response:
  statusCode: 201
  statusText: Created
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "name": "string",
        "schema": "string",
        "id": "string"
      }
order: 1000
