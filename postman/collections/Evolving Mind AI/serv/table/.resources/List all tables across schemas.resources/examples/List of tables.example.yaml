$kind: http-example
request:
  url: http://{{baseUrl}}/serv/table?schema=string
  method: GET
  headers:
    Accept: application/json
  queryParams:
    schema: string
  body:
    type: text
    content: ""
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      [
        {
          "name": "string",
          "schema": "string"
        },
        {
          "name": "string",
          "schema": "string"
        }
      ]
order: 1000
