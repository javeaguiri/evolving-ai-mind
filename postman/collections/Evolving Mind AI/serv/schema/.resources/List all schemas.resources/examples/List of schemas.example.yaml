$kind: http-example
request:
  url: http://{{baseUrl}}/serv/schema
  method: GET
  headers:
    Accept: application/json
  body:
    type: text
    content: ""
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      [
        {
          "name": "string",
          "tables": [
            "string",
            "string"
          ]
        },
        {
          "name": "string",
          "tables": [
            "string",
            "string"
          ]
        }
      ]
order: 1000
