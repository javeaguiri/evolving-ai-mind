# evolving-mind-ai — Backlog

Active tech debt register, tangential feature designs, and build history. Items in architecture.md §7 and §15 were moved here to keep architecture.md focused on active decisions.

---

## 0. Active Task List

Mirrors the in-session TaskCreate list. Recreate at the start of each new session with TaskCreate so tasks are trackable. Last updated: 2026-05-11.

| # | Status | Task | Notes |
|---|--------|------|-------|
| 1 | ↩️ reverted | Change queryId to use PGC_Session.id integer | Reverted by commit `fd69d46` — diagnostic notifications show UUID query_id so integer regex broke all /explain commands; UUID_RE restored across all three files |
| 2 | ✅ done | Fix Ask Follow-up button in /explain reply threads | (1) `proc/explain.mjs`: restored `queryId: session.query_id` in HUMAN_NOTIFICATION. (2) `interactive.mjs`: `handleExplainFollowupButton` now calls `slack.chat.update` to replace the stale button before opening the modal. Completed tasks 3/4/5/7 moved to `docs/backlog-history.md` |
| 8 | ✅ done | Run PGC_SystemContext.content JSONB migration | content→jsonb, format column dropped. New schema.mjs endpoints: modifyColumn + dropColumn. seed rewritten to sections schema. |
| 9 | pending | Validate analyze_and_design_workflow field name fix | Prompt id 25; response_format + v10 deployed session 23 — not yet validated |
| 10 | pending | Add PGC_WorkflowRun.session_id FK column | Migration script needed — column did not exist at bootstrap |
| 11 | pending | Add Tier 1 post-write validation after workflow writes | After fix_workflow step 8 / create_workflow step 19, run L1 simulation and fail on dead routing targets |
| 12 | ✅ done | Domain propagation — systemic audit (W3, Sprint 5) | All 6 system boundaries audited. B1–B5 fixed in prior sprints and covered by domain-propagation.test.mjs. B6 (`deriveCallScope` in llm-harness.mjs — memory retrieval + save scope during llm_call steps) exported and covered by 6 new unit tests asserting priority order, fallback, and scope.domain omission for system workflows. 358 unit tests pass. |
| 13 | ✅ done | Add L1 check: serv_* steps must declare required input fields | Added `serv_step_missing_required_input` check in `runLevel1StaticAnalysis`. Validates `serv_query` (`tableName`), `serv_insert` (`tableName`, `row`), `serv_update` (`tableName`, `filters`, `updates`), `serv_delete` (`tableName`, `filters`). 4 existing unit tests updated to use correct step definitions. 32/32 tests pass. |
| 14 | ✅ done | Remove stale Level 3 references from architecture.md and system context seed | architecture.md §15 lines 3817/3884 updated; stale `skip_path_warnings` removed from §6.5.6 result shape. `seed_PGC_SystemContext.json` simulate description updated (v8→v9); pushed to DB via `upsert-system-context.mjs`. |
| 15 | ✅ done | Report malformed output_key (non-string) as L1 error | `runLevel1StaticAnalysis`: when `output_key` is present but not a string, raises `malformed_output_key` issue. Same guard added for option-level `output_key`. L2 skip guards remain as-is since L1 now blocks them. |
| 16 | ✅ done | Investigate why simulate did not detect the missing serv_query tableName (run 323) | Root cause: L1 checked routing, template vars, iterator source, and gate cancel options, but had no serv_* required-field check. The LLM was given the `input.tableName: required` rule via `step_type_contracts` but omitted it; the simulation had no safety net. Fixed by task 13. |

---

## 1. Tech Debt — Active

Items are unresolved unless otherwise noted. ✅ items were resolved mid-session and are archived in git.

### High Priority

| Item | Notes |
|---|---|
| **`create_domain` records derived-field maintenance rules that nothing consumes — every denormalized column in every domain is unmaintained (2026-08-04)** | **The case.** `PGD_Decks.card_count` read 0 on deck 16 (`C1/C2 Unidad 1`) while the deck held 34 cards. The other decks are correct only because Novia repaired them by hand on 2026-07-07, before deck 16 existed — so the correct-looking rows are evidence of a manual backfill, not of a working mechanism. **The instruction is not missing.** `PGC_Memory` 142 (`semantic`, `scope: {domain: flashcards}`) is the only one of ~12 flashcards schema snapshots matching the live tables — verified column-by-column against `PGD_Decks` and `PGD_Cards` — and it states the rule in full: *"PGD_Decks.card_count: Set to 0 at deck creation. Increment by 1 when a card is added to this deck; decrement by 1 when a card is removed or deleted. **Updated via application logic after card insert/delete**."* `create_domain` emitted the convention correctly. **What is missing is the consumer.** `add_entity` is a generic system workflow that inserts one row into one table; nothing reads a domain's initial-value conventions at insert time and turns "increment the parent's counter" into a step. The rule is prose addressed to an application layer that does not exist. **This is not specific to `card_count` or to flashcards** — memory 142 delegates `learned_count`, `due_count` and `last_review_date` to the same absent layer (deck 16 reads `learned_count: 0`, `due_count: 0` against 34 cards carrying `next_review_date`), and any domain `create_domain` builds with a denormalized column inherits the same hole. **Fault domain: Contract, at the `create_domain` boundary** — not Instruction (the instruction is correct) and not Execution (the harness is not being handed anything it rejects). **Three candidate fixes, none decided.** (a) **Do not denormalize.** At household scale `card_count` is `COUNT(*)` at read time or a `PGC_Schema` view (`type`/`select_sql` already exist) — `design_table` would need a rule against derived columns whose maintenance it cannot generate. Cheapest and removes the class. (b) **Generate the maintenance.** If a domain declares a derived column, `create_domain` must also emit the workflow steps that maintain it, so the rule ships with its consumer instead of as a note. (c) **Make the conventions machine-readable** and have `add_entity` apply them — the largest change, and it turns a prose memory into a contract the engine depends on, which is the direction `CLAUDE.md` warns about unless the shape is genuinely standard. **Sequence (a) first**: it asks whether the column should exist before building machinery to maintain it. **Adjacent finding, same investigation:** ~12 `Schema snapshot for flashcards (confirmed)` memories coexist, all `scope: {domain: flashcards}`, all priority 2–3, with mutually contradictory table and column names (`PGD_Deck` vs `PGD_Decks`, `name` vs `title`, `PGD_Flashcard` vs `PGD_Cards`) — leftovers from abandoned `create_domain` runs. Only 142 matches the live schema. Anything retrieving "the flashcards schema" from memory draws from a dozen conflicting answers with no way to tell which is live, and the priority ties make which ones surface non-deterministic (see the transcript-cost entry). Reinforces the deferred `PGC_Memory` semantic-deduplication / TTL item. |
| **A prose reply forfeits the round's prefix credit, because it is stored with no `items` (2026-08-11)** | **The remaining half of the typed-reply restart fix.** `b982c99` made a typed follow-up arrive as the `function_call_output` of the pending `respond` call rather than as a trailing user item, and it is **validated live**: session 1151 turn 3 read 9,787 tokens against turn 2's `inputTokens` of 9,788 — ratio 1.00, where the forfeit would have pinned `read` to the instructions+tools block. **But it keys off the gateway `items` persisted on the respond entry, and one path never has them.** When the model answers in prose without calling `respond`, `minds-eye.mjs` logs *"text reply with no tool call — treating as respond"* and builds the entry with `entryItems = []` (the ternary at the top of the decision loop yields `[]` when `entry.call` is null). `toInputItems` then falls back to the older `{role:'assistant', content}` rendering, the next typed reply is pushed as a `{role:'user'}` item, and that turn re-creates the whole transcript at 1.25×. **Verified on the live session rather than inferred:** seq 3 carries 1 item, seq 5 carries 0 — and seq 5 is a prose reply. **Cost is the same as the original defect** ($0.219 per occurrence at a 56k transcript, growing linearly), so the fix is only as good as how often she calls `respond` properly, which is not something to leave to chance. **Fault domain: Execution.** **Fix:** synthesise the `respond` call at the point the prose reply is persisted — a `function_call` item with a locally-generated `call_id` and `arguments` of `{message}` — so the entry carries items like every other. This is the one case `toInputItems` cannot recover from persisted state alone, because the gateway never sent a call to replay. Note the synthesised-`call_id` path already exists there for pre-`items` entries and states the constraint: ids need only be unique and consistent inside the array, and cannot collide with real gateway ids, which carry a provider prefix. **Adjacent question worth settling at the same time:** whether a prose reply should be accepted at all, or answered with a tool-use reminder — it currently works by a deliberate leniency (*"treat it as the reply it plainly is, rather than failing the round on a technicality the user would never see"*), which is right for the user and is now also a silent cost. |
| **`register_workflow` accepts an `llm_call` naming a prompt that does not exist (2026-08-10)** | **The case (workflow 358, `process_receipt`).** Novia registered a 24-step workflow that passed L0 + L1 + L2 — shape, routing and data flow all valid — whose steps 3 and 10 call `parse_receipt` and `match_inventory_items`. **Neither exists in `PGC_Prompt`.** The workflow is live, intent-routed on ten phrases, and dies at step 3 with `prompt not found: intent_category="parse_receipt"` the first time anyone triggers it. **Nothing in the validation stack looks at prompt existence:** L0 composes its assertions from `PGC_StepType.input_contract`, which requires `llm_call.input.prompt` to be *present*, not to *resolve*; L1 and L2 are pure functions over the step array with no I/O by design. So the one cross-artifact reference an `llm_call` makes is the one nobody checks. **Why it matters more than it looks.** This is the AC3 failure mode exactly — *a build that registers but does not run is a FAIL at any price* — and it is silent at the only moment it could be caught cheaply. The user's next signal is a dead run, by which point the session that could have fixed it in one turn has ended. **The annotation exists and its consumer does not — corrected 2026-08-10 after reading the contract.** The first write-up of this item proposed "refuse when the prompt is missing", which is the wrong fix on its own. `PGC_StepType.llm_call` already declares two optional fields: `prompt_category` (*"snake_case slug for a domain-specific prompt not yet in PGC_Prompt. Must equal input.prompt"*) and `prompt_draft` (*"Full prompt text for a new domain-specific prompt… **Consumed by `design_workflow_prompts` to insert a new `PGC_Prompt` row.** Required when `prompt_category` is present and the prompt does not yet exist"*). **Novia supplied both, correctly, on both `llm_call` steps of workflow 358** — 1,383 and 1,544 characters of prompt text, sitting in the registered row right now. She read the registry and did exactly what it says. **`design_workflow_prompts` is a `create_workflow`-family step, and the `register_workflow` path bypasses `create_workflow` entirely**, so on her path the declared consumer does not exist — and `create_workflow` is retired by the sprint direction, meaning `prompt_draft`'s only consumer lives on a path we tell people not to run. This is the same shape as `create_domain` emitting derived-field maintenance rules nothing consumes, and the fourth instance of Sprint 9's recurring finding that a correct registry read produces a wrong outcome when the registry describes machinery that is not there. **Fix, in order:** (1) `register_workflow` consumes `prompt_draft` — insert the `PGC_Prompt` row as part of registration, which is what the contract already promises the author; (2) **then** refuse when a referenced `intent_category` resolves to nothing and no draft was supplied. `register_workflow` already performs I/O (it writes `PGC_Workflow` and `PGC_IntentMap`), so unlike the simulation levels it may read: one `getRows` against `PGC_Prompt` filtered `intent_category IN (…)` regardless of step count. **Fault domain: Contract** for the dangling annotation, **Validation** for the missing refusal — both, not one. **Where these prompts live is settled, and it is not the seed.** A domain prompt for a user-created workflow is an **evolving artifact**: it belongs in `PGC_Prompt` and nowhere else. Putting `parse_receipt` in `seed_PGC_Prompt.json` would push receipt, grocery and inventory vocabulary into the system's own seed data, which CLAUDE.md forbids outright — *"User domain data must never appear in system artifacts."* Its durability story is the RDS backup that covers the `PGD_*` rows it operates on, not configuration control. The seed-update convention (*edit the seed, then upsert*) governs **seeded system rows**; it does not reach rows the system creates at runtime. The same line applies to `register_workflow` consuming `prompt_draft` above: it writes to the database, and nothing in that flow should touch a seed file. **Scope question, decide when building:** the same gap exists for other cross-artifact references a step array can make — `serv_query.tableName` against `PGC_TableMap`, `prompt` on any step type that takes one — and the honest framing is that **`register_workflow` validates the array in isolation and never validates it against the system it will run inside**. Recommend building it as a named reference-resolution pass rather than a prompt-specific `if`, so table names can join it without a second mechanism. Keep it in `register_workflow` (and any future equivalent gate), **not** in `simulation-engine.mjs` — that module is pure by contract and this check needs the database. **Related, same root:** `upsert-workflow.mjs`'s pre-write guard runs the same simulation levels and would accept the same workflow from a seed file. |
| **The `js_transform` sandbox object reads as an allowlist and is not one — analyse the real contract (2026-08-10)** | **Partially fixed; the analysis is the open part.** The immediate defect is closed: `PGC_StepType.js_transform` described its own environment as a *"pure* synchronous JS IIFE… 200ms timeout"* when the engine runs it for **500ms** and deliberately provides `Date`. That word "pure" is why dates got hardcoded twice (`import_budget_spreadsheet` D3, `process_receipt` step 3): an author who takes it seriously will not call `Date.now()`, concludes the current date must arrive from outside the transform, and passes it in as a step input — where the only thing to put is a literal. **The author's reasoning was correct and the contract's was wrong.** Description corrected, and an advisory L1 `frozen_date_literal` check added. **What remains is the wider question this exposed.** `runSandboxedExpression` builds `sandbox = { JSON, Math, Array, Object, String, Number, Boolean, Date }`, which *looks* like a curated allowlist and is not one — `vm.runInNewContext` hands the script a fresh V8 context that already carries every ECMAScript intrinsic, so `parseFloat`, `RegExp`, `Map`, `Set`, `Intl`, `Promise`, `Symbol` and `BigInt` all work without appearing in it. The contract itself proves the list is decorative: it instructs authors to use `parseFloat()`, which is absent from it. The **real** boundary is the AST denylist — `require`, `eval`, `fetch`, `XMLHttpRequest`, `Function`/`new Function`, member access on `process`/`global`/`__dirname`/`__filename`, plus `import`/`await`/`async`. **Three things to settle:** (1) **Is a denylist the intended model?** It is permissive by default, so every future ECMAScript intrinsic is admitted automatically and silently. A `vm` context cannot reach the host without the blocked globals, so this is defensible — but it should be a decision on the record, not an artefact of how `vm` works. (2) **Delete the misleading sandbox object or make it real.** Passing eight intrinsics that V8 supplies anyway is a no-op that reads as a security boundary; either drop it with a comment explaining that the AST gate is the boundary, or freeze the context so the list becomes true. (3) **`Date.now()` and `Math.random()` make `js_transform` non-deterministic**, which is correct for a date and awkward for anything replayed — `js_transform` is not fingerprinted today (only `llm_call` is), so nothing breaks, but a workflow whose branch depends on `Math.random()` cannot be reproduced from its run record. Worth a stated position. **Fault domain: Contract** for the description (fixed), **open design question** for the model. |
| **Domain validator — check a created domain against the conventions nothing currently enforces (2026-08-10)** | **The gap.** `create_domain`'s output is validated for *shape* (Ajv against the prompt's `output_schema`) and its DDL either executes or does not. Nothing checks whether the resulting domain is any **good**: that its declared foreign keys actually reached the registry, that names follow the system's own conventions, that a column called `title` in one table is not `name` in the next. Every defect below was found by a human reading `PGC_Schema` by hand after the fact. **Cases observed on the inventory domain (run 764), each of which a validator would have caught in one pass:** (1) **FK declared, never registered.** `create_domain` emitted `{"name":"fk_itemlocations_parent","column":"parent_id","references":{"table":"PGD_ItemLocations","column":"id"},"onDelete":"CASCADE"}` and `PGC_Schema.constraints` for that table holds one unique and two checks and **no FK**. Whether the physical constraint exists is unverified — there is no route to `pg_constraint` through the SERV API, which is itself part of this item: the validator needs one. The registry asserting *less* than the database is the Sprint 9 invariant inverted, and it is invisible to every consumer that reads the registry to understand the domain. (2) **FK column naming has no stated convention.** `parent_id` on a self-referencing hierarchy against `category_id` → `PGD_SpendingCategories` elsewhere: the house style is FK-names-the-target-singularised, which for a self-reference yields `item_location_id` and reads worse. The validator's job is not to pick — it is to *notice the divergence and say so*, because right now nothing does. (3) **Enum treatment is inconsistent within one table.** `status` got `status IN ('new','good','damaged','expired','unusable')`; `unit` (default `'pieces'`) and `category` (default `'Uncategorized'`) got nothing. A column with a small closed value set and no CHECK is a free-text column in every form the system renders, because enum-driven field rendering reads `PGC_Schema.constraints`. (4) **Column-name vocabulary drifts across domains** — `name` / `title` / `description` / `notes` are used interchangeably for the same role. `schema-utils.mjs`'s `pickLabelColumn` already has an opinion about which column stands in for a row; the validator should hold the domain to it. (5) **Derived columns with no consumer** — `item_count`, `level`; see the `create_domain` derived-field entry above, of which this is the detection half. **Shape of the fix, undecided.** Most likely a new `/proc` endpoint (`validate-domain`) over `PGC_Schema` + `PGC_EntitySchema` + live DDL introspection, returning findings the way `simulation-engine` returns issues — severity-tagged, never auto-applied. Whether it runs as a `create_domain` step before the final gate (catches it at creation, costs a step) or on demand (catches existing domains too, needs invoking) is open; **recommend both — one engine, two entry points**, exactly as `runSimulation` serves `create_workflow`, Novia and `upsert-workflow.mjs`. Novia should get it as a tool. **Fault domain: Validation** — this is the missing layer, not a defect in any existing one. **Note the precedent for how findings should read:** L1's `dead_routing_target` told Novia routing was broken seven times when one field name was wrong nineteen times; a domain validator that reports twelve naming findings for one convention mismatch will be ignored. Group by cause, not by occurrence. |
| **create_workflow — query row-limit discipline (Sprint 9)** | **The case (run 735):** `load_summary_data` set `limit: 13` on a summary view holding ~21 rows per period, intending "13 recent months"; ordered by `year` alone the limit grabbed 13 rows of one month and **dropped the current month entirely**, which then rendered as an all-zero placeholder. The view had full data — the query truncated it. **Rule (`design_workflow_process`):** a row `limit` is an assertion about how many rows you need — set one only when the number is justified against the expected dataset shape; if you cannot justify it, **omit it**. A limit smaller than the data the workflow actually needs drops rows *silently* (a silent wrong answer), whereas an unbounded read is cheap at household scale and, if it overflows a display, is caught loudly by the L1 gate-size check. Corollary (optional, keep in reserve): if you do limit, order by the full key that determines which rows survive. Fault domain: Instruction. |
| **edit_budget — bold `**` renders literally in a gate (Sprint 9, needs repro)** | Reported on run 735: standard `**bold**` (and/or a `##` heading) shows its markdown symbols instead of rendering, inside a human_gate message. Experience-tier rendering (`callback.mjs`), fault domain Execution. Session 12 applied `toSlackMrkdwn` to notifications and reveal/label sites, but a gate message path may still emit `**` into a legacy `mrkdwn` block (needs `**`→`*`), or a `##` heading is landing in a plain_text `header` block (which cannot render inline emphasis). **Needs the exact screen + line to pin which block path** before fixing. |
| `WORKFLOW_ERROR` tells the user a count, not the problem | **The case (run 717).** A `create_workflow` run died at step 11 and the user saw: *"LLM output validation failed after 2 attempts (2 schema errors). The prompt has been logged for improvement."* That is a number, not a diagnosis. It names no field, no step, no cause, and no next action. The actual error was sitting right there in the AJV output and is perfectly renderable in one sentence: *"the gap analysis proposed a schema change using fields that aren't allowed (`change_type`, `column`)."* **The system knew exactly what was wrong and told the user an integer.** Same shape as Sprint 7's M7/M9 — an error path that technically fires but delivers nothing the user can act on. "The prompt has been logged for improvement" is *technically* true (`llm-harness.mjs` writes the full attempt history to `PGC_Session` unconditionally, even on validation failure — that is how run 717's raw output was recovered) but the user is never told this, is not told `/explain 717` exists, and has no reason to believe anything will come of it. It reads as a shrug. **Fix (`postWorkflowError` in `callback.mjs`, Experience tier — the summarisation already lives there):** parse the AJV errors it already receives and say what failed in the user's terms — the offending field(s), and where. Keep the count as a suffix if useful, not as the message. Surface the `/explain <runId>` affordance explicitly rather than implying an invisible improvement process. **Apply the non-technical-user test when writing it:** the reader does not know what AJV, a schema, or `additionalProperties` is; they know they asked for a budget editor and got a shrug. Related: the `isValidationError`/`isLlmError` branches in `postWorkflowError` currently regex the error *string* to classify it — worth checking whether the structured error is available instead, rather than re-deriving from prose (checklist rule 2e). |
| `create_workflow` — confirm the domain, never infer it silently | **The case (run 716).** User typed `/create-workflow edit workflow` intending "edit budget". No domain matched, so `classify-intent` set `input.domain: null` — which is a **legitimate, supported value**: Mode C, a standalone workflow with no domain tables. Nothing errored. `create_workflow` silently took the "you meant a standalone workflow" branch and every downstream stage (`domain_schema` → empty, `workflow_mode`, `research_workflow_domain`, `analyze_workflow_gaps` Mode A/B/C, the whole design) proceeded correctly on that premise. It was not a failure — it was **a confident answer to a question nobody asked the user.** `domain` is the single most load-bearing input in `create_workflow`; getting it wrong does not throw, it quietly builds the wrong workflow. **Fix:** a `choice` gate at the very top. If a domain resolved — *"Building a workflow for `budgets_expenses`. Is that right?"* — confirm or pick another. If none resolved — list the existing domains **plus an explicit "None — this is a standalone workflow"** option. The load-bearing principle: **"no domain" must be a deliberate choice, not a default.** Mode C is real and worth keeping, but it should be opted into, not fallen into on a typo. Fire the gate **always**, not just when resolution fails — a *mis*resolved domain is just as damaging as an unresolved one and equally silent. Options list is one small `serv_query`; past five entries `choice` renders as a dropdown on its own (Track L1), so it scales. **Implementation constraint (important):** `run-workflow.mjs:148` hardcodes the root frame's `current_step: '1'`, so every workflow MUST have a step literally keyed `"1"` — the gate cannot be a "step 0". Verified that nothing back-edges to step 1 in `create_workflow` (it is entry-only), so the clean insertion is: the new domain gate **becomes** step `"1"`, and the current step 1 (`serv_query` → `domain_schema`) is re-keyed (e.g. `"1c"`) as the gate's `on_success` target. No global renumber, no routing churn. **Open question worth deciding first:** how the confirmed value reaches the ~8 steps that read `{{input.domain}}` — either write back via `output_key: "input.domain"` (`setPath` supports dot paths; zero downstream edits, but `local_state.input` then diverges from the immutable `PGC_WorkflowRun.input` record) or introduce a separate `domain` local_state key and repoint those steps (more churn, no new convention). Recommend the latter: "the domain the user confirmed" is genuinely a different fact from "the domain the classifier guessed", and after Sprint 7 we should be wary of anything that quietly rewrites an input record. **Related harness wart, not in scope:** hardcoding `'1'` as the entry step is itself a naming-convention leak — a cleaner harness would start at `steps[0]`. |
| A render failure in the Experience tier should fail the run, not just report it | Sprint 7 / M9 made `callback.mjs` classify a permanent Slack rejection (`invalid_blocks`, `msg_too_long`, …), tell the user in plain text, and stop retrying — instead of logging it, retrying 3×, and losing it to the DLQ while the user stared at an empty thread. But the **run itself still wedges**: `/proc` suspended it at `awaiting_human_gate` and has no idea the gate never rendered, so it sits there forever (run 711 had to be marked `failed` by hand). Closing this needs a channel from EXP back to PROC — `callback.mjs` imports neither SERV nor SQS **by design** (transport-agnostic Experience tier, see `architecture.md` §3.1), so this is an architectural decision, not a patch: either (a) EXP enqueues a `GATE_RENDER_FAILED` message onto WorkflowQueue — cheapest, but gives the Experience tier its first outbound queue dependency; (b) PROC treats an un-acked gate as failed after a timeout — no new coupling, but needs a sweeper and a notion of gate liveness; or (c) PROC validates renderability **before** suspending — cleanest in principle, but it would put Slack's 50-block limit inside `/proc`, which is exactly the domain-vocabulary-in-the-wrong-tier violation the Experience/Procedure split exists to prevent. **Recommend (a)**, with the message treated as a UI-agnostic "the experience layer could not present this gate" signal rather than anything Slack-specific. |
| Validate that every `llm_call` step supplies every `{{token}}` its prompt declares | An unresolved `{{token}}` in a `PGC_Prompt`'s `prompt_text` renders **as itself** — the LLM is handed the literal string `{{redundancy_findings}}` and nothing anywhere reports a problem. This is the root class behind three separate Sprint 7 bugs: **M4** (`design_workflow_dialogs` had never been given `template_syntax`, so it emitted `<angle brackets>` the resolver printed verbatim), **N1c** (`run-workflow` dropped a null output, the key vanished from `local_state`, and `design_workflow_process` received `"{{user_workflow_feedback}}"` as the user's feedback), and the one found while verifying N1c: `create_workflow` steps 21 and 21t **share** the `design_workflow_process` prompt, and each omitted a token the other supplied — so the first-pass design was told `{{redundancy_findings}}` in the very block that reads "non-null means this is a CONSOLIDATION PASS". A literal token is non-null. Fixed pointwise (explicit `null` in each step's input for the channel it doesn't use), but nothing prevents the next instance. L1's `unresolved_template_variable` check only inspects the workflow step's own templates — it never compares a prompt's declared tokens against the input map of each `llm_call` step that uses it. Fix: at `upsert-prompt.mjs`/`upsert-workflow.mjs` time (and/or as an L1 check), extract every `{{token}}` from `prompt_text`, and for each `llm_call` step referencing that prompt, assert the token is either supplied in `input` or declared optional-with-default. Shared prompts (one prompt, several calling steps) are where this bites, and they are not rare. Consider also making an unresolved token in an **LLM-bound** string fail loudly rather than pass through — silently feeding a template token to a model is never correct. |
| ✅ **RESOLVED (Sprint 9 A2)** Condition step routing format — reconcile docs to match engine behaviour | **Resolved by describing the engine rather than picking a canonical form.** The `condition` contract in `PGC_StepType` now states that a bare key and the `step:N` form are both accepted and identical after normalisation (`step-executor.mjs:1637`, `run-workflow.mjs:1711` strip the prefix), as does `human_gate.options[].on_select`. Choosing one form would have invented a constraint the engine does not have. The remaining seed disagreements (`workflow_routing_rules`, `workflow_constraints`, `step_usage_patterns`) are `inject_for` the create_workflow family only and are retired with those prompts. Original note: Engine (`cda93df`) accepts both bare step keys (`"3"`) and `step:N` format (`"step:3"`) for condition `on_success`/`on_else`; `step-executor` normalises both identically. But guidance is inconsistent across seeds: `workflow_routing_rules` says bare keys only (NEVER `step:N`); `workflow_constraints` says `step:N`; `step_usage_patterns` says bare keys only; `step_type_contracts` says bare keys only; the output schema `routingToken` pattern accepts both. Pick one canonical form, update all prompts and system contexts to state it, and optionally update `workflow_routing_rules` to drop the NEVER clause. The choice is cosmetic — either form works at runtime. |
| `step_type_contracts` dual-maintenance — explore unification with `PGC_StepType` | `PGC_SystemContext/step_type_contracts` (LLM-teaching layer) and `PGC_StepType` (execution layer) both define the same step type specifications and are kept in sync manually via seed files. The gap revealed in Sprint 7: `{{step_type_contracts}}` was never wired into `generate_workflow_steps`, so the LLM never saw required fields (e.g. `execution_mode` on `iterator`). Workaround: token added to prompt (v40). Explore: should `assembleInstructions` build the LLM-facing contracts dynamically from `PGC_StepType` rows at runtime, eliminating the hand-maintained copy? Design questions: (1) Is the current two-layer split intentional — PGC_StepType as machine contract, PGC_SystemContext as pedagogical/curated LLM description? (2) If auto-derived, do PGC_StepType rows contain enough teaching-quality prose for effective prompt injection, or does curation add real value? (3) Migration path — deprecate `step_type_contracts` SystemContext row and have `assembleInstructions` query `PGC_StepType` instead. Prerequisite for any change: verify no prompt currently relies on SystemContext content that diverges from PGC_StepType. |
| `reveal` multi-level hierarchy support | Current `reveal` / `reveals` implementation is one-level-deep only — parent panels each render a flat text list of children. Deeper hierarchies (e.g. region → country → city, course → chapter → lesson) require either: (1) nested `reveals` arrays rendered recursively in the Slack UI, or (2) a paginated drill-down pattern (select a parent → gate re-fires showing only that parent's children). The recursive approach requires Block Kit support for nested expandable sections; the drill-down approach is implementable today with condition + loop steps. Scope the correct pattern when a genuine two-level use case arises. |
| Remove /chat dead code (obviated by Novia) — Sprint 8 | `/chat` and `/explain` were designed as a simpler Q&A companion layer before Novia existed. Novia's agentic loop handles conversational questions, diagnostics, and fixes in a single interface — `/chat` no longer adds distinct value. Remove: (1) `/chat` and `/explain` slash command handlers in `slackbot/`; (2) `session_type = 'chat'` branch in session logic; (3) `/chat` and `/explain` references in `openapi.yaml`; (4) `arch-session.md` `/chat`-specific sections. Keep: `PGC_Session`/`PGC_SessionEntry` tables (used by Novia), `session_type = 'minds_eye'` path, `handleExplainFollowupButton` pattern (adopted by Novia). Audit `chk_pgc_session_type` constraint — remove `chat` and `explain` values after confirming no live sessions use them. |
| Checkpoint / revert for Novia write sessions and repair steps | Novia's inline write tools (`update_data`, `insert_data`, `fix_workflow_steps`) and repair paths (`fix_workflow`, `troubleshoot_workflow`) execute immediately with no undo path. A bad column value, corrupted workflow step, or repair loop that overshoots can only be recovered via manual SQL. Fix: before any Novia session that performs a write action, snapshot the affected rows (or full tables for small PGD domains + PGC_Workflow) into a `PGC_Checkpoint` table tagged with `session_id`, `triggered_by`, and `snapshot_at`. Expose a `/novia revert last` path (or a Slack button on the session-end notification) that restores from the most recent checkpoint. **Design questions:** (1) Granularity — per-session snapshot (cheap, coarse) vs per-write-action (precise, noisy for iterative sessions). Recommend per-session: snapshot once at first write, label it with the session. (2) Scope — snapshot only the rows Novia touched (requires tracking touched table+id pairs) vs a domain-level dump. Recommend touched-rows tracking: `executeTool` records `{ table, filter }` for each write; checkpoint captures the before-image of those rows only. (3) Retention — keep last N checkpoints per session or per domain; expire after 7 days. (4) Revert UI — button on turn-limit and session-close notifications; also invocable by follow-up prompt "revert that". **Prerequisite:** `PGC_Checkpoint` table with `id`, `session_id`, `snapshot_at`, `rows jsonb` (array of `{ table, pk, before_image }`). Revert executes a `serv_update` per row using the before-image. |
| `create_workflow` — multi-path workflow entry | Workflows are currently designed with a single start path (e.g. `start_quiz` always opens with deck selection). Novia and intent-classify can invoke workflows with context already known (deck id, entity id, etc.). `create_workflow` needs to generate an optional-input condition at the start: if the triggering context supplies a value, skip the selection/query steps and jump to the first action step. Applies to any workflow with a "pick X" first step — quiz deck selection, entity picker, etc. This affects both `create_workflow` prompt design and the HUMAN_GATE bypass pattern. **Instruction fault domain.** |
| Lambda death resilience — pre-write `started` step record | **Root cause (run 422):** when a Lambda is killed mid-step (e.g. LLM call hits the 115s abort ceiling, then cleanup async calls exhaust the remaining 5s before Lambda timeout), no `PGC_WorkflowRunStep` row is written. On SQS retry, `checkIdempotency` returns false → step re-executes → same cycle for 32 minutes until manually cancelled. **Interim fix (Sprint 4):** Lambda timeout widened to 240s + SQS VisibilityTimeout to 600s, giving 125s cleanup headroom. **Proper fix:** (1) Add `'started'` to the `chk_runstep_status` check constraint via ALTER TABLE + update `PGC_Schema` seed. (2) Write a `started` record to `PGC_WorkflowRunStep` immediately before `executeStep`. (3) On SQS retry, `checkIdempotency` finds the `started` record → stuck-step guard fires → run fails cleanly after 3 hits. No schema changes needed to `run-workflow.mjs` stuck-step logic — it already handles repeated idempotency hits. |
| `PGC_IntentMap` — one row per phrase (structural refactor) | Current model concatenates all invocation phrases into one pipe-delimited regex string per workflow (`"test me with my flashcards\|review_flashcards"`). This makes phrases opaque and non-updatable: adding or removing a single phrase requires string surgery on the whole field, there is no provenance (which phrase came from the user vs. auto-generated), and the FK `workflow_id` is written on the combined row rather than per phrase. Fix: store **one row per phrase** — `{pattern, intent_category, action_type, workflow_id, source}` where `source` is `user \| auto \| name`. Adding a phrase becomes a plain `insertRow`; removing one is a targeted `deleteRow`; the FK is always set. `create_workflow` steps 35b/36 simplified to one insert per phrase. `matchIntentMap` unchanged — it already iterates rows and tests each pattern. `PGC_Workflow.intent_keywords` folds into this model as additional rows, eliminating the two-store split. Prerequisite: migration to split existing combined patterns into individual rows. |
| Stack overwrite race condition — activate `PGC_WorkflowRunLock` | A long-running Lambda (e.g. 55s LLM call + correction loop) loads the run stack at t=0, then writes it back at t=1m45s — overwriting gate frames pushed by other Lambdas in between. Reproduces when: (1) a step with a slow LLM call is followed closely by a human_gate in the same run, and (2) the gate is suspended before the slow step's Lambda finishes. Root cause: `updateRows('PGC_WorkflowRun', ..., { stack: run.stack })` uses no compare-and-swap. Fix: activate `PGC_WorkflowRunLock` with optimistic locking — load `version`, include `version` in the WHERE clause of every stack update, retry on conflict. `PGC_WorkflowRunLock` table and column exist; the harness never writes to them. First seen in run 384. |
| Perplexity budget exhaustion — user-facing error and proactive warning | When Perplexity returns HTTP 402 (Insufficient credits) or 429 (budget exhausted), the current error bubbles as `Agent API error 402: {"error":"..."}` in the WORKFLOW_ERROR notification — not user-friendly and gives no remediation path. Two fixes needed: (1) `llm-client.mjs` — detect 402/budget error bodies and throw a named error with a clear message: "LLM budget exhausted — please top up your Perplexity account to continue." (2) `run-workflow.mjs` WORKFLOW_ERROR path — detect `budget_exhausted` error type and post a human-readable notification with a direct link to the Perplexity billing page instead of a generic failure message. Nice-to-have: proactive warning at 80% budget via a daily EventBridge check against Perplexity's usage API (if available). |
| Surface prompt `intent_category` + `id` in workflow error notifications | Error notifications for failed LLM validation steps currently say "prompt has been logged for improvement" with no actionable reference. Surface `intent_category` and prompt `id` in the Slack error message so the user can hand both to `/chat` with memory context (`scope: intent_category`) to diagnose and fix the prompt directly. Prerequisite for the Track I Novia agentic prompt-repair loop. |
| LLM validation errors belong in `PGC_WorkflowRunStep`, not `PGC_Prompt.error_log` | **Current state:** `review-output.mjs` appends structured error entries (AJV errors, failing values, output excerpt) to `PGC_Prompt.error_log` — a shared JSONB array on the prompt row. **Problems:** (1) Errors from all workflow runs pile up in one array with no run or step reference; Novia cannot trace an error back to a specific run. (2) `PGC_Prompt` is a config table, not an audit table — attaching operational error history to it conflates two concerns. (3) `error_log` grows unbounded (no retention/eviction). **Correct location:** `PGC_WorkflowRunStep` already captures per-step execution records linked to both `PGC_WorkflowRun.id` and the step key. Add an `llm_error` jsonb column to `PGC_WorkflowRunStep` and write the validation failure there instead. `PGC_Prompt` retains only the latest schema issues needed by the improvement loop (keep `last_error` as a single entry, not an array). **Side benefit:** Novia can then read the exact run-scoped error via `query_table PGC_WorkflowRunStep` given a `run_id` — no inference needed. **Prerequisite:** `PGC_WorkflowRunStep` audit log write path must be fixed first (see "PGC_WorkflowRunStep audit log not written" item). |
| Workflow safety guards (velocity detector, execution accumulator, cycle detector) | Required before production. Right-brain can monitor `PGC_WorkflowStats` for anomalous run patterns and flag suspect workflows proactively |
| Duplicate domain detection — LLM runs every time | `/create-domain recipes` re-runs the LLM even if the domain already exists. Fix: add a `serv_query` pre-check step to `create_domain` workflow before the `llm_call` |
| Guard 3 cycle detector — backward reference handling | Guard 3 must distinguish gate-bounded loops from tight computational loops. Rule: a backward reference is safe if the path from target back to source contains at least one `human_gate` step |

| `design_table` prompt — type-matched literals in check constraints | The `design_table` (and `generate_domain_schema`) prompt allows the LLM to combine `real`/`float` column types with decimal boundary constraints, producing schemas that fail at runtime. Root cause (run 458): `difficulty_level real` with `difficulty_level >= 1.3` — PostgreSQL evaluates `1.3` as `numeric`, the `real` column stores `~1.29999995`, comparison fails. Fix by **replacing** two sections of the prompt rather than adding new ones: **(1) Strengthen the column type rule** — append to the existing type list: "In check constraint expressions, cast numeric literals to match the column type. For `real` columns use `::real` casts (`difficulty_level >= 1.3::real`). For integer columns bare integers are fine. For text/varchar comparisons use single-quoted strings with no cast." **(2) Replace the standalone constraint expression rule** — merge it into the type requirements: "Check constraint `expression` must use type-matched literals: `real` columns require `::real` casts (`difficulty_level >= 1.3::real`), integer columns use bare numbers (`age >= 18`), text/varchar use quoted strings (`status IN ('active', 'inactive')`). This co-locates the type casting rule with the type definition rule, provides concrete examples, and avoids separating 'define types' from 'use types correctly' across two prompt sections. Update `seed_PGC_Prompt.json` (`design_table`), run `upsert-prompt.mjs`. |

| ✅ **RESOLVED** Novia `buildUserMessage` action menu — move to system prompt | Done as described. `buildUserMessage` now pushes only the one-line reminder ("decide your next action… respond with exactly one JSON object per the tool catalog and output format in your instructions"); the catalog lives once in `minds_eye_system_prompt`. The in-code note records the reason it was removed rather than patched: the second copy had already drifted stale (missing `run_sql`, `upsert_data`, `create_view`, `drop_view`) while still telling the model to use only the tools it listed. Original note: The available-actions list (action names, gate policy, parameter shapes) is appended to every user-turn LLM call in `buildUserMessage`. It never changes within a session yet is re-sent on all 8+ turns — pure token waste that compounds with session length. Fix: move the action contract into the `minds_eye_system_prompt` PGC_SystemContext seed (seed update + upsert, no code change). |
| **Novia's reasoning round dies silently when the Lambda times out — `turn_limit` is unreachable (Sprint 9, AC6)** | **The case (session 1121, 2026-08-01T09:18:13→09:22:13).** `runReasoningLoop` runs up to `turn_limit` turns **inside one Lambda invocation**, and ProcFunction's `Timeout` is 240s. Observed per-turn latency in that round: 7s, 85s, 46s, then a fourth call still running when Lambda hit its wall — `Duration: 240000.00 ms, Status: timeout`. So an invocation affords **three or four turns**, and `turn_limit: 12` cannot be reached by wall clock no matter what the counter says. B3 raised it 8 → 12 believing turns were the binding constraint; they are not. **The failure is completely silent.** A Lambda timeout is not an exception — nothing catches it, so no `HUMAN_NOTIFICATION` is posted, no `PGC_SessionEntry` row is written for the turn in flight, and `minds_eye_turn_count` is never updated because it persists only at round end (session 1121 still reads `turns: 9` with its last entry at 09:20:31). The SQS message was already deleted at handler start (`deleteReceivedBatch`, `architecture.md` §3.2), so there is no retry either — the work is simply gone. From Slack it is indistinguishable from Novia hanging: the last progress line appears, then nothing, forever. The in-flight LLM call is still billed. **Fault domain: Contract** — a budget denominated in the wrong unit, with no guard on the unit that actually binds. **Fix (a), recommended, cheap:** a wall-clock guard in the loop. Before starting a turn, compare elapsed time against a round budget; if the next turn cannot plausibly finish (the longest single call is bounded by `LLM_TIMEOUT_MS`, 170s, plus the session writes after it), end the round the way the turn-limit gate already does — persist `turnCount`, post the continue gate, let the user resume. The round then ends *legibly* instead of vanishing, and the existing resume path is reused rather than a new one invented. **Prefer a configured `round_budget_ms` in `minds_eye_preferences` over `context.getRemainingTimeInMillis()`** — PROC endpoint modules are transport-agnostic by rule (§3.5) and must not learn they are running on Lambda. **Fix (b):** persist `minds_eye_turn_count` per turn rather than at round end, so a lost round still counts against `max_lifetime_turns`; today a session can burn turns that the lifetime ceiling never sees. **Open design question, decide before implementing (a):** should a round instead *continue across invocations* — re-enqueueing MINDS_EYE to resume the loop automatically when the budget runs out — rather than stopping for a human continue? That would make `turn_limit: 12` real, at the cost of a session that keeps spending without a human checkpoint, which is the thing the continue gate exists to prevent. Recommend the gate for now: a build that has run three turns and paused is legible; one that has run twelve unattended is not. |
| ✅ **RESOLVED (Sprint 9, B1 follow-up)** L0 does not validate the universal step envelope — a step array with no `step` keys passes shape (Sprint 9) | **The case (session 1121, entry #17).** Novia simulated a 19-step array in which every step carried **`step_label`** instead of `step`; all 19 step keys resolved to `null`. **L0 passed it** (`shape_analysis: { ran: true, passed: true, issues: [] }`). L1 then emitted seven `dead_routing_target` issues each reading `Step "undefined" field "…" routes to "6" but no step with key "6" exists` — truthful and useless: every routing target looked dead because `stepKeys` (`simulation-engine.mjs:413`) was the single-element set `{"undefined"}`. She was told routing was broken seven times when the defect was one field name repeated nineteen times. She recovered unaided on the next turn, so the cost was a turn and an LLM call, not the session. **Root cause:** `runLevel0ShapeCheck` composes every assertion from `PGC_StepType.input_contract`, which is exactly what makes it maintenance-free — but `step` and `type` are **universal envelope fields, present in no type's contract**, so the single most load-bearing field in the format is the one field L0 never checks (`run-workflow.mjs` compares `frame.current_step` against it). **Fix:** L0 asserts the envelope before dispatching to the per-type contract — `step` present, a non-empty string, and unique across the array; `type` present. Four structural rules, not a per-type list, so the maintenance-free property is preserved. This is what B1's "validate, then simulate" is for and it should have been one line of output instead of seven misleading ones. **Fault domain: Contract.** **Related instruction gap, fix together:** `workflow_convention_bridge` says *"Each element is a step object carrying a step key, a type, and routing fields"* — it describes the step key and never names the JSON field `step`. `step_label` is a reasonable reading of that sentence. One clause naming the field closes it. Fault domain **Instruction**. |
| **`register_workflow` should refuse a domain workflow with no `intent_keywords` (Sprint 9)** | Workflow 357 (`edit_budget`) registered with `intent_keywords: null`. Not a tool bug — session 1121 shows Novia passed `["budget","edit","amounts"]` on the failed attempt (#23) and omitted the field on the successful one (#26); the gate warned (*"No intent_keywords — Pass 2 keyword routing will not match this workflow"*) and was approved anyway. **The consequence is misrouting, not silence.** `matchWorkflowByKeywords` (`classify-intent-tiers.mjs:115`) builds its candidate set as `r.domain === domain \|\| r.domain === null`, so the generic CRUD workflows compete inside every domain — and `update_entity` claims `["update","edit","modify","change"]`, exactly the verbs a budget edit uses. A workflow with no keywords cannot win a scan it is nominally part of. Pass 1 (`matchIntentMap`) is a regex substring test, so `"edit budget"` matches `"edit budget amounts"` but **not** `"edit the budget"` — and that phrasing falls through to Pass 2 and lands on the generic row editor instead of the purpose-built form. Options: derive keywords from the `intentPhrases` already supplied (they are right there in the same call), or treat the omission as an error when `domain` is non-null. A `domain: null` system workflow legitimately has none — `ping_core` has `[]` — so this is a domain-workflow rule, not a universal one. |
| **`run_sql` gives Novia no route to physical table names (Sprint 9)** | Seven of roughly twelve `run_sql` calls in session 1122 failed on identifiers: `PGC_WorkflowRunStepLog` invented twice, `pgc_workflow_run_step_log` invented once, `PGC_WorkflowRunStep` written unquoted (Postgres folds it to lowercase → relation does not exist), and a `workflow_name` column that does not exist. She recovered by probing `information_schema` twice. Roughly **$1.50 of that session's $3.40** spent discovering table names. `list_physical_tables` already exists as a read tool; what is missing is (a) a pointer to use it before writing raw SQL and (b) the rule that these CamelCase identifiers must be **double-quoted**, which is the single most common failure above. Both are prompt/context content, not code. Note the same session shows the tool working well once the names are right — this is a discoverability gap, not a capability one. |
| **Evidence for a cancelled run is thin, and the guidance points at the wrong place (Sprint 9)** | Two failures compound. **(1) A cancel discards local_state.** `run-workflow.mjs:190` and `:529` both write `{ status: 'cancelled', stack: [], completed_at }` — the stack is cleared and `state` is never written, so a cancelled run keeps `stack: []` and `state: {}`. Only the completion path (`:544-552`) writes `state: { local_state }`. Runs 739/741 have no recoverable local_state at all. Consider writing the final frame's `local_state` on cancel exactly as completion does — a cancelled run is often the one you most want to inspect. **(2) `PGC_WorkflowRunStep` snapshots are capped** at `slice(0, 200)` (`:431`) and `slice(0, 100)` on the iterator/nested paths (`:1421`, `:1544`), which shows a step's shape and a prefix but not its values — enough to see that a query returned rows, not enough to see whether they were right. **(3) Novia's diagnostic guidance covers live runs only.** It directs her at `stack`/`state`, correct while running and empty once finished. The rule she needs is three-way: running → `stack[top].local_state`; completed → `state.local_state`; cancelled → neither, read `PGC_WorkflowRunStep`. That belongs in a `PGC_SystemContext` row she already fetches, not in code. |
| **Novia's conversation history is re-sent whole every turn — cost scales with transcript length (Sprint 9, AC9)** | **Mechanism.** `minds-eye.mjs` calls `callLlm`, which sends exactly `{ model, input, instructions, max_output_tokens }`. `buildUserMessage` flattens Layer 1 + Layer 2 context **and the entire transcript** into that single `input` string — `User: …` / `Assistant: …` / `Tool (name): {json}` sections joined by `---`, each tool result capped at 15,000 chars. There is no messages array. **And that is deliberate:** `llm-client.mjs:172-177` records that the Agent API's `messages` history field "is only honoured by sonar models. Non-sonar models (anthropic/*, openai/*) routed through the gateway silently ignore it — the model only receives `input`." Novia runs `anthropic/claude-sonnet-4-6`, so a messages array would be dropped; `callLlmWithMessages` already applies the same flatten-into-`input` workaround for `/chat`. **Measured (session 1121, 2026-08-01, the B4 build):** 14 calls, **$0.8372**, avg $0.0598, per-turn cost climbing `0.018 → 0.029 → 0.043 → 0.047 → 0.057 → 0.061 → 0.073 → 0.079 → 0.070 → 0.105` and the truncated turn at **$0.208 for nothing**. The shape is unambiguous in the usage logs: `cache_read_input_tokens` sits pinned at **4041 every single turn** — the stable system-prompt prefix — while `cache_creation_input_tokens` climbs `4473 → 2761 → 7374 → 10996 → 11755 → 14523 → 15427 → 17505 → 21525`. The growing transcript is re-written to cache at creation price every turn, and creation is the expensive class: on the failed call, `cache_creation_cost` $0.084 against `cache_read_cost` $0.001. **This is not fixed by raising a budget** — it is a cost characteristic of the assembly, and the session reached $0.84 against the `create_workflow` baseline of ≈$1.42 per paid build *before the workflow was designed*. **The transcript is dominated by tool results**, not by reasoning: the `PGC_StepType` query returned all 19 rows with full contracts, and one later `human_gate` re-query alone is 13.4 KB — so re-reading a registry row pays for it twice, and every turn after pays again. **Candidate directions, none decided:** (a) supersede rather than accumulate — a repeat query of the same table+filter makes the earlier result dead weight; keep the latest and drop the prior from working history. (b) Project registry reads — `getRows` already accepts a `columns` whitelist and Novia asks for every column of every step type when she needs three rows; the narrowing belongs in the system prompt's query examples, not in code. (c) Compress *within* a round — session compression exists at the turn-limit gate (§6.1) but nothing trims while a round is running, which is exactly when the transcript grows. (d) Re-test whether the gateway now honours `messages` for `anthropic/*`; the comment is a dated observation about a third party and is the cheapest thing here to re-check. **Not a defect — a design decision.** Sequence (b) first: it is data, not code, and it shrinks the input at the source rather than repairing it downstream. **Update 2026-08-01 — this is now blocking, not merely expensive.** Session 1122 (the repair session, not the build) reached `input_tokens: 58,444` with `cache_creation_input_tokens: 54,401` on a single turn — creation price on a 54k-token transcript, $0.28 per turn — and cost **$3.40 without completing the repair**, on top of $2.73 for the build. Two of that session's failures were transcript-driven rather than reasoning-driven. Sequence this ahead of the remaining Sprint 9 items. **Update 2026-08-04 — diagnosed. It is a cache-invalidation defect in our own code, not a gateway limitation, and the four candidate directions above were all treating the symptom.** The reading of "`cache_read` pinned at 4041" as *only the system prompt is cacheable* was wrong: tokens land in `cache_creation` **only if a breakpoint covers them**, so the climbing `cache_creation` proves the gateway *is* caching the transcript — writing a fresh entry every turn and reading none of it. Prompt caching is a **prefix match**: any byte change invalidates everything after it. The match dying at exactly 4041 every turn means the first bytes of `input` differ turn to turn. Two causes, both in `minds-eye.mjs`. **(1) The volatile content is in front of the stable content.** `buildUserMessage` (`:1570`) orders `input` as Layer 1 context → Layer 2 context → transcript. The transcript is the append-only part whose prefix is stable; the context blocks are re-fetched every turn and sit ahead of it. **(2) Layer 2 ordering is non-deterministic.** `assembleContext` (`:1518`) runs `ORDER BY priority DESC LIMIT 5` on `PGC_Memory` with no tiebreaker — and **35 of the table's 100 rows are tied at priority 8** (verified live 2026-08-04). Postgres does not guarantee a stable order for ties, so which five rows return, and in what order, can differ between identical queries. That block sits near byte zero of `input`, so a reshuffle invalidates the whole transcript behind it. This is the textbook silent invalidator — non-deterministic serialization ahead of cached content. **The fix, three parts, all in system code and none dependent on Perplexity:** (i) add a deterministic secondary sort key (`id`) to both `assembleContext` queries — smallest change, probably the largest share of the effect; (ii) reorder `buildUserMessage` so the transcript comes first and the context blocks last, making turn N's input turn N−1's input plus an append; (iii) stop rewriting history in place — the draft-supersession branch (`:1598-1602`) mutates an earlier entry when a new array is submitted. (iii) is lowest priority: it invalidates only from that entry onward, on submit turns, where (i) and (ii) invalidate everything on every turn. **Expected payoff:** cache reads cost ~0.1× base input against 1.25× for creation, so converting the transcript from creation to read is roughly a 12× cut on that component — nearly all of session 1122's $3.40. Turns run 7–100s apart, well inside the 5-minute TTL, so entries stay live between them. **Caveat:** we do not control where the gateway places breakpoints, so the exact cached boundary is unknown — but the prefix instability is ours, is certain, and makes a hit impossible however the gateway is configured. **Deferred to Sprint 10 by the user (2026-08-04):** the change touches input assembly for every Novia session and needs a live Slack round to validate, which the user runs; landing it untested with no test environment is the risk the branch-to-prod process exists to avoid. Directions (b)/(c) above still stand but are second-order — they shrink the payload; this makes resending it nearly free. |
| **Novia — resume a response severed at the output ceiling (Sprint 9)** | `llm-client.mjs:133` names the correct remedy and stops short of it: *"A correction prompt won't help; a resumption will."* Sprint 9 added the cheap half — `classifyLlmFailure` returns `reask`, and the same question is put again with the cut-off stated (never the severed output echoed back, since its length is the problem), once only. What is still unbuilt is genuine **resumption**: sending the partial output back as an assistant prefix and asking the model to continue from it, so a long answer completes across two calls instead of being retried from scratch. Worth building only if the re-ask proves insufficient in practice — measure first. **Note the hard coupling before raising any limit:** `max_output_tokens` is bounded by `LLM_TIMEOUT_MS` (170s), whose own comment warns a timeout destroys the whole run. Session 1121 generated 8192 output tokens in ~104s, so the timeout permits roughly 13.4k output tokens at that rate; the ceiling was raised 8192 → 10240 and stops there. Going materially higher needs `LLM_TIMEOUT_MS` **and** ProcFunction's `Timeout: 240` raised together — and a timeout carries no `isTruncated` flag, so it gets no re-ask and fails hard. Raising output alone trades a recoverable failure for an unrecoverable one. |
| Novia workflow invocation catalog — resolve duplication concern | Sprint 5 implemented both: (a) `read_workflow` now returns `description` alongside steps, and (b) `minds_eye_system_prompt` includes a static system workflow catalog (which workflows Novia may/may not trigger). These overlap — `description` on `read_workflow` is discovered per-read; the catalog in the system prompt is always present. Evaluate after a few sessions: if Novia reads descriptions reliably before deciding to trigger, remove the static catalog from the system prompt and let `description` carry the invocation model. If Novia skips reads and still mis-triggers, the catalog is load-bearing and should stay. |
| `create_domain` — PGC_DomainHelp embedding not computed on insert | When `create_domain` inserts the `PGC_DomainHelp` row via `serv_insert`, the `embedding` column is left NULL. SERV's read-before-write embedding path requires an `embed_source` field in the `updateRows` payload to trigger — `insertRow` does not trigger it automatically even though `PGC_Schema` defines `embed_source: ["domain","description","aliases"]`. Result: every new domain is undiscoverable by Novia (`search_domain_help`) and the intent classifier's semantic Pass 2 until `backfill-embeddings.mjs` is run manually. **Immediate workaround:** run `node dev_scripts/backfill-embeddings.mjs` after any `create_domain` run. **Systemic fix:** either (1) extend `table.mjs` `insertRow` to trigger embedding on insert when `embed_source` is defined in the schema, or (2) add an explicit `updateRows` step to `create_domain` immediately after the `serv_insert` DomainHelp step to force the embedding. Option 1 is correct long-term (extend-not-prompt principle); Option 2 is a workflow-only patch. |
| `create_domain` — reference table entity separation | **Symptom (run 473):** `/m add these categories…` routed to `add_entity PersonalBudget`, which has `PGD_Accounts` as root and `PGD_Categories` as an orphan child (no FK to accounts). The LLM correctly put category data in `children.categories` and left `root: {}`. The `parse_entity_input` output schema requires `root.minProperties: 1` → validation failed after 2 attempts. **Root cause:** `create_domain` does not distinguish **reference tables** (standalone lookup data — categories, tags, statuses, types) from **transactional tables** that depend on them (transactions, budgets, reviews). A reference table needs its own entity schema and add/list/update/delete workflows. A transactional entity schema (e.g. `PersonalBudget`) should only include tables that genuinely have an FK to the root table. **Open design questions (resolve in Sprint 6):** (1) Scope — is a reference table domain-scoped (only visible/usable within `personal_budget`) or system-wide (shared across domains, e.g. a `categories` reference shared by both `personal_budget` and `expenses`)? (2) FK threading — when a user adds a transaction, how does `add_entity` resolve a `category_id` FK to a row in a separate reference entity? Currently `serv_entity_insert` only threads FKs within the same entity schema. (3) `parse_entity_input` output schema `minProperties: 1` on root — should be relaxed to `0` to support children-only inserts in general, but requires `serv_entity_insert` to handle empty root (skip root insert, just insert children with known FKs). **Sprint 6 scope:** update `create_domain` (research + design steps) to classify tables as reference vs transactional and generate separate entity schemas accordingly. Define FK cross-entity threading strategy. Decide reference table scope. |
| `research_workflow_domain` — surface raw/degraded input format; open question on where the fix belongs | **Original scope (Sprint 7 Track G2, deferred):** when a workflow's intent involves parsing copy-pasted spreadsheet data, receipt photos, or other degraded-format input, `research_workflow_domain` should emit a finding flagging the expected raw shape — section headers doubling as category/type labels, collapsed columns, headings arriving as standalone plain-text lines. This feeds `design_workflow_process` → `design_notes` for the parsing step → `generate_workflow_steps`'s prompt_draft, completing the chain A7 (done) starts from the translation side. Trigger: keywords like "paste," "photo," "receipt," "import," "spreadsheet" in the user's request. **Open design question surfaced 2026-07-05:** comparing the live `import_budget_spreadsheet` workflow (id 349, generated by `create_workflow` run 626) against generic `add_entity` found that budget's FK-resolution steps (5–11: query existing categories → diff missing → gate-confirm → insert → reload → remap ids) are structurally identical to `add_entity`'s existing generic ref-FK chain (steps 2a–2f) — hand-rolled instead of reused. The *only* genuinely domain-specific content in the whole workflow was the parsing prompt (`parse_spreadsheet_budget`, which encodes section-header/enum/currency-symbol knowledge) and the `(year, month, category_id)` composite match key passed to `serv_upsert` — both prompt/config data, not new logic. This suggests one-off ingestion workflows like this one exist mainly because `add_entity`'s generic `parse_entity_input` isn't yet trusted with domain-specific parsing rules for degraded input — the same gap G2 was meant to close, just aimed at `generate_workflow_steps` instead of at `parse_entity_input`. **Before implementing G2, decide:** should the raw-input-format signal feed `generate_workflow_steps` only (original narrow scope, keeps one-off workflows as the mechanism), or should it (also/instead) harden `parse_entity_input` so `add_entity` can absorb degraded bulk input directly — which would let workflows like `import_budget_spreadsheet` collapse into "call `add_entity` with a domain-aware parse prompt swap" and avoid duplicating the FK-resolution logic in generated workflows going forward. Related, still-unsettled musing: whether such one-off/bulk data loads are better handled by Novia's agentic loop (gated `insert_data`/`upsert_data`, human confirm, C7's validate-before-presenting SOP) rather than a persisted `create_workflow` artifact at all — plausible for genuinely one-off loads, but loses L1/L2 simulation validation, so likely wrong for recurring ingestion paths. |
| **Track: UI Polish — dialog rendering and pretty-print output (Sprint 7)** | Remaining sub-items ((A) markdown block type and (B) `format_entity_display` are done — see `docs/sprints/CURRENT.md` Track D1/D2): **(C) Audit all `notify` templates across generated domain workflows** — identify templates emitting raw JSON or unformatted field dumps; update `generate_crud_workflows` and `design_workflow_dialogs` prompts to produce markdown-rich output by default so new workflows emit clean messages without post-hoc fixes. No changes to `serv/entity.mjs` or the Slack renderer. **(D) Button/content separation and content-preservation audit** — enforce a structural convention in all gate-rendered messages: content blocks (section, markdown, description_list) and action blocks (buttons) must be separate `blocks` array entries. On any button click or dialog submission, filter out only the `actions` block and call `chat.update` with the remaining content blocks intact — never replace the full message. Audit all interactive handlers in `slackbot/` (`handleExplainFollowupButton`, `handleMindsEyeFollowupButton`, all gate response paths) and apply the pattern consistently. Update `design_workflow_dialogs` prompt to always emit content and buttons as separate blocks so generated workflows follow the convention from creation. **(E) Modal cancel audit** — audit all `on_modal_close` handling: confirm `handleViewClosed` does not enqueue `resume_gate` (gate stays suspended, correct); remove `on_modal_close` dead code branch from `run-workflow.mjs`; scan all `PGC_Workflow` rows for `on_modal_close` option declarations and remove them. |
| Novia reasoning loop — per-turn Slack progress notification | While Novia is looping through reasoning turns (calling `read_workflow`, reading data, etc.) the user sees nothing in Slack until the session ends or the turn limit fires. Sessions with 8+ silent turns appear broken. Fix: after each `read_tool` or tool execution in the reasoning loop, post or update a Slack ephemeral/threaded "Novia is thinking…" message showing the current turn number and last action taken (e.g. "Turn 3/8 — read_workflow: quiz_flashcards"). On session end or gate, replace the progress message with the final output. Options: (1) Edit a pinned "thinking" message in-place each turn (single message, low noise). (2) Post a new brief line each turn (noisy but transparent). Option 1 preferred — call `chat.update` on a persistent message ts stored in session state. Requires `PGC_Session` to store the `progress_ts` Slack message timestamp. |
| Novia LLM API resilience — configurable repair vs structural escalation | **Context:** Perplexity gateway tightened validation for Anthropic models (`max_output_tokens` now required), breaking the Novia reasoning loop. The fix was a code change to `minds-eye.mjs`. **Analysis:** API changes fall into two categories: (1) *Configurable values* (model ID, token limits, endpoint URL) — these belong in `minds_eye_preferences` system context; Novia can already update system contexts via `update_data` and can self-repair these without a deploy. (2) *Structural call changes* (new required fields absent from a `callLlm` site, new auth header, changed HTTP method) — these require a code change + deploy; Novia can diagnose and surface the error but cannot fix them. **Immediate actionable items:** (a) Improve the reasoning-loop error handler to parse the 400/402 body and post an actionable Slack message identifying the field/rule that failed, not just the raw error string. (b) Add `endpoint_url` and any future LLM gateway config values to `minds_eye_preferences` so Novia can update them without a code change. (c) For category (2) failures: the error surfaces in Slack today — no automated fix path is needed, but consider a `/novia test llm` diagnostic tool that makes a minimal test call and reports pass/fail + error detail. **Deferred (evaluate in Sprint 7+):** `PGC_Capability` table for tracking non-LLM external API dependencies. The LLM call itself should not be modeled as a capability — it is the substrate Novia runs on, not a tool she inspects. Circular dependency: a broken LLM API cannot be diagnosed by an LLM. |

| `get_entity` search column — convention enforced, backfill flashcards | `get_entity` step 3 now hardcodes `"column": "title"` (changed from `"name"`). `pgd_naming_conventions` system context mandates `title` as the primary label column on all root tables; reference/lookup tables use `name`. Existing domains created before this rule need their label column renamed if it isn't `title`. Known affected domain: flashcards (`PGD_Flashcards.term` — rename to `title` or confirm `term` is intentional and handle separately). |
| Session ID per workflow run — PGC_SessionEntry persistence for all LLM calls | Regular workflow runs (`triggered_by: slack \| api \| workflow`) have `session_id: null` — no LLM input/output written to `PGC_SessionEntry`. Novia cannot access the run-scoped call history; `/explain` cannot link steps to entries. **Fix:** assign/generate a `session_id` for every `PGC_WorkflowRun` at start and write `PGC_SessionEntry` rows for every `llm_call` step regardless of `triggered_by`. Prerequisite for `/explain` step-selection gate and Novia run-scoped diagnostics. Requires `PGC_WorkflowRun.session_id` FK column migration (active task 10). **Blocking budget/expense testing via Novia. Sprint 7.** |
| `/explain <run-id>` step-selection gate | When `/explain` is invoked on a failed run with multiple LLM steps, present a Slack button list of the run's `llm_call` steps (step key + intent_category label); user picks one; spawns a fresh explain thread scoped to that step only. Existing single-step behaviour preserved when the run has exactly one LLM step. Another `/explain` call on the same run opens an independent thread for a different step. **Prerequisite:** session ID per workflow run (item above). Fault domain: Execution. **Blocking budget/expense testing via Novia. Sprint 7.** |
| PGC_SystemContext procedure library for Novia | Add `novia_diagnostic_protocol` row to `PGC_SystemContext` with detailed on-demand diagnostic steps (read `error_log`, query `PGC_WorkflowRun`, inspect LLM call history, propose fix path). Add one-liner to `minds_eye_system_prompt`: "For detailed diagnostic and modification procedures, query PGC_SystemContext by key before proceeding." Procedure definitions evolve as data with no code change or redeploy. Keeps the always-injected system prompt lean. **Sprint 7.** |

| `run-workflow.mjs` hardcoded `current_step: '1'` on root frame init | Line 148 initialises the root frame with `current_step: '1'` regardless of which step key is first in the workflow's `steps` array. Any workflow that starts with a step keyed `"0"` or anything other than `"1"` silently skips those steps. Fix: replace the hardcoded `'1'` with `steps[0]?.step ?? '1'` — requires moving the `loadSteps` call before the stack-init block (currently loaded after). Low blast-radius change; all existing workflows start at `"1"` by convention so behaviour is unchanged for them. |
| `create_domain`/`design_table` — compound unique constraint inference + `serv_upsert` native `ON CONFLICT` (Sprint 8) | **Depends on Sprint 7 Track H** (`serv_upsert` step type, implemented as a query-then-write workaround since no table currently declares compound unique constraints — see run 623 diagnosis). No table in the system has a compound unique constraint today; `design_table` (within `create_domain`) has no rule for recognising natural/composite keys on tables where uniqueness is defined by a combination of columns rather than `id` (e.g. `PGD_Budgets` unique per `(year, month, category_id)`; recurring-period, dedup-on-import, and any "one row per (X, Y, Z)" domain shape). **Sprint 8 scope:** (1) `design_table` prompt rule — detect and declare composite unique constraints during schema design (e.g. from phrasing like "one budget per category per month," or inferred from the workflow's own upsert-key usage). (2) `schema.mjs` DDL support for `UNIQUE (col1, col2, ...)` constraints at table-creation time (and via `dropConstraint`/an `addConstraint` endpoint for existing tables). (3) Upgrade `serv_upsert` (`table.mjs`) to use native `INSERT ... ON CONFLICT (matchColumns) DO UPDATE` when the target table has a matching constraint registered in `PGC_Schema`, falling back to the Sprint 7 query-then-write path otherwise — decided by capability, not by workflow author. This hardens the harness over LLM prompt discipline: once the constraint exists, correctness no longer depends on the LLM getting `matchColumns` right at every call site — the DB enforces it. |
| UC-E4 budget report — `create_workflow` + DB view (Sprint 7) | Blocked in Sprint 6 on no real data. Approach: (1) Create `PGD_MonthlyExpensesByCategory` DB view manually (`GROUP BY category_id, SUM(amount) WHERE date_trunc('month', date) = date_trunc('month', NOW())`); register in `PGC_Schema` + `PGC_TableMap` so `serv_getRows` can query it. (2) Use `create_workflow` to generate the reporting workflow: `serv_getRows` on view → `serv_getRows` on `PGD_Budgets` → `llm_call` to format comparison as readable Slack output. LLM for formatting only — no arithmetic. (3) Validate end-to-end from Slack once expenses + budgets have sufficient data. `createView` SERV endpoint (backlog Medium) enables future automation of step 1. |
| `read_memory` step type — conditional memory injection for `design_workflow_process` (and possibly other modes) | `design_workflow_process` currently has a blanket `memory_config` (600 tokens, semantic) that fires on every call regardless of `workflow_mode` — a stopgap. Its retrieved memory (the `schema_snapshot`/`insert_expectations` row `create_domain` writes) is only actually useful in write mode; read/analyze/enrich modes pay the injection cost for content they can't use. **Correct fix:** a new `read_memory` step type in `step-executor.mjs` that calls `memory-client.mjs`'s `retrieveMemories` explicitly from within a workflow's own step sequence (`scope`/`memory_types`/`budget_tokens` in, formatted block written to `output_key`), gated by a preceding `condition` on `workflow_mode` and wired into `create_workflow`'s own steps immediately before the `design_workflow_process` `llm_call` — feeding the result in as an explicit `{{domain_insert_expectations}}` input variable, visible and debuggable in the workflow JSON, unlike the invisible per-prompt auto-injection the other `memory_config`-using prompts rely on. Once live, remove `design_workflow_process`'s blanket `memory_config` in favor of the conditional step. **Broader open question to resolve as part of this work:** does `create_domain` currently capture enough about *why* a domain exists and *how* its data is meant to be consumed — aggregation/reporting intent, typical query shapes, which columns are analysis-relevant vs. purely operational — to usefully inform read/analyze-mode workflow design the same way `insert_expectations` informs write mode? If so, that implies a second, distinct semantic memory `create_domain` should write (its own tag, e.g. `usage_patterns`) and a second conditional `read_memory` injection point gated on `workflow_mode IN (read, analyze)` — scope this alongside the write-mode fix rather than assuming `insert_expectations` is the only domain memory worth conditionally injecting. |
| Novia's `assembleContext()` — consider unifying with `retrieveMemories` scope-aware retrieval | Novia's own Layer 2 memory-context assembly (`minds-eye.mjs`, `assembleContext()`) calls `getRows('PGC_Memory', [], { column: 'priority', direction: 'desc' }, 5)` directly — no `memory_type` filter, no scope filter, just the 5 globally highest-priority rows across every domain, labeled "RECENT MEMORIES" regardless of actual recency. This is a second, independent memory-loading implementation entirely separate from `memory-client.mjs`'s `retrieveMemories`/`expandScope` (the scope-aware path every `llm_call` step's `memory_config` uses) — the same duplicate-logic-can-drift pattern already hit twice this sprint (`markdown_formatting_syntax`, `buildUserMessage`'s stale tool catalog). `retrieveMemories` already has a `callContext: 'chat'` mode (includes `{topic: 'persona'}` scope) that looks aimed at exactly this use case but was never wired up to it. **Open questions before unifying:** (1) what call scope should a Novia session use — a workflow run has one fixed `{domain, workflow}`, but a conversational session doesn't map onto a single domain/workflow the same way; (2) priority direction mismatch — `assembleContext()` sorts priority `desc` (higher number shown first) while `retrieveMemories` sorts `asc` (lower number = higher priority) — the two systems currently disagree on what the `priority` column means, and that needs reconciling before any unification. |

| `pull-prompt.mjs <intent_category>` will import a domain prompt into the system seed (2026-08-10) | The no-arg form is safe by design — it pulls only *categories already known in the seed*, so runtime-created domain prompts cannot leak in. The **named** form has no such guard: `node dev_scripts/pull-prompt.mjs parse_receipt` writes a user-domain prompt into `seed_PGC_Prompt.json`, putting receipt/grocery/inventory vocabulary into the system's own seed data — the thing CLAUDE.md forbids in "User domain data must never appear in system artifacts". Nothing warns; the diff looks like a normal prompt addition and would plausibly survive review. **Found because it was recommended in error and the user caught it** — which is the argument for a guard rather than a convention. **Fix:** the row already carries the discriminator. Prompts created for a user workflow have a non-null `domain` and `workflow_name` (`parse_receipt` = `{domain: "inventory", workflow_name: "process_receipt"}`), while system prompts have neither. Refuse to pull any row with a non-null `domain`, naming the reason. One condition, and it makes the boundary enforced instead of remembered. **Fault domain: Execution.** Consider the symmetric case too: `upsert-prompt.mjs` would happily push such a row back if one ever reached the seed. |

### Medium Priority

| Item | Notes |
|---|---|
| **Novia and `/chat` sessions never close — decide what "open forever" should cost** | **The affordance is right; the implementation is what needs bounding.** A Slack thread can be replied to weeks later, so a session that refuses to reopen would fight the medium it lives in. Keep it open. The problem is that resuming replays *everything*: `toInputItems` rebuilds the item array from **every** `PGC_SessionEntry` row in the session, so a thread answered three months on re-sends its entire transcript, and the gateway's cache TTL is long expired — so turn 1 of that resume pays creation price (1.25x) on the whole history. The only backstop today is `max_lifetime_turns: 100`, which tells the user to start a fresh session and carries nothing across. **Second, less obvious consequence: staleness.** `assembleContext` refreshes Layer 1/2 per round, but the *transcript* freezes what the system looked like when it was written — workflow versions, schemas, step-type contracts. A months-old session resumes with a confident and wrong picture of the registry, which is the "correct read of an incomplete registry" failure with time as the cause. **Directions, none decided:** (a) compact on resume past a size or age threshold — distil superseded turns into a summary entry, keep recent turns verbatim; this is carry-forward item 4 (session compression), which 2c's cheap turns finally make reachable. (b) Age-based fork — resuming a session older than N days opens a NEW session seeded with a summary plus `PGC_Memory`, linked by `parent_session_id`, so the thread stays replyable but the cost restarts. (c) Do nothing and let `max_lifetime_turns` handle it — cheapest, but it discards context at exactly the moment the user signalled they still wanted it. **Recommend (a) with (b) for age specifically**, since the two triggers are different: size is a cost problem, age is a staleness problem, and a summary fixes both only if it is re-derived rather than replayed. Raised 2026-08-09. |
| **`design_table` has two `PGC_Prompt` rows (v1 and v15)** | Noticed 2026-08-09 while tracing `create_domain` step 13. `getRows('PGC_Prompt', [{ intent_category: 'design_table' }])` returns two rows. If `llm-harness` selects by `intent_category` without ordering, which one is served is not guaranteed — the same defect class as the `PGC_Memory` priority tie, and worse in consequence: an old prompt version silently authoring domain schemas. **Check first whether this is real** — the selection may already order by version, and the v1 row may be an orphan from a pre-versioning upsert. If it is real, the fix is one of: delete the orphan, or add an explicit ordering + tiebreaker to the prompt read. Cheap to verify, potentially expensive to leave. |
| **Evaluate the `review_workflow_redundancy` critic — does it actually cut anything?** | Added Sprint 7 (session 17) after the user observed that `design_workflow_process` produces overly complicated, hard-to-maintain workflows. Run 700 was the specimen: a 21-step `edit_budget` whose steps 15+16 (a `js_transform` + `condition`) existed only to work out which button the user had clicked — something `on_select: "step:<key>"` does in zero steps. The pipeline is additive by construction (`analyze_workflow_gaps` enumerates gaps → `design_workflow_process` turns each into steps; nothing ever merges or deletes), and nothing gives the LLM a cost signal for step count. Fix shipped as a **critic, not a rewriter**: new `review_workflow_redundancy` prompt emits *findings only* and never authors a design, so `process_design` keeps exactly one author (avoiding the duplicate-logic-drift pattern that bit `markdown_formatting_syntax`/`buildUserMessage`, Novia's `assembleContext`, and the `callback.test.mjs` copies). Findings feed back into `design_workflow_process`'s own revision path; a `condition` bounds it to exactly one pass (no loop). **This is an unproven heuristic and must be measured, not assumed.** Evaluation data needs no new instrumentation — `PGC_SessionEntry` (B2) already records the full response of *both* design calls, so step count before (step 21) and after (step 21t) is directly recoverable per run, alongside the findings themselves. **Questions to answer over the next several `create_workflow` runs:** (1) does it find anything, or return empty every time? (2) when it finds something, does the revision pass actually *remove* the steps, or acknowledge and keep them? (3) any false positives — did it ever propose cutting a load-bearing step (watch for L2b data-flow failures appearing only after consolidation)? (4) is `sonnet-4-5` warranted, or is `haiku-4-5` sufficient for what is largely pattern-matching against concrete tests? **Precedent to respect:** Sprint 4's `runRoutingValueRules` was a heuristic validation check whose false positives made it net-negative, and the correct fix was deleting it entirely. If this critic proves to be noise, delete it rather than tuning it indefinitely. |
| Reconcile Slack `markdown` block heading-size discrepancy (docs vs live render) | Found 2026-07-06 while implementing Sprint 7 Track D2: Slack's own docs (docs.slack.dev/reference/block-kit/blocks/markdown-block) state *"all header levels are rendered at the same size"* inside a `markdown` block, but a live test rendering one `markdown` block containing `#` through `######` in sequence showed `#`/`##`/`###` each at a visibly distinct size — only `####`/`#####`/`######` collapsed to a shared size. Not yet reconciled: is the doc simply wrong/stale, or does behavior depend on client (desktop vs mobile vs web), theme, or something else not tested? Needs: a few more controlled live renders (isolate one heading level per block; test across clients) before treating either the doc or the one-off observation as authoritative. Notes so far captured in `docs/slack-block-kit.md`'s Markdown block section — do not remove that caveat until this is actually resolved. Low urgency: doesn't block anything, since this project's `markdownToBlocks()` splitter already converts `#`-`######` into real `header` blocks (genuine H1-H4 sizing, capped at 4) rather than relying on the `markdown` block's own inline heading rendering. |
| Recursive self-referential entity fetch — arbitrary-depth hierarchies (e.g. deck-within-deck) | Surfaced while scoping Sprint 7 Track D2 (`format_entity_display`). `PGC_EntitySchema.joins`/`aggregations` only model one level of child-table joins (e.g. `PGD_Decks` → `PGD_Cards`) — there is no support for a self-referential join (`PGD_Decks.parent_id` → `PGD_Decks.id`), so `get_entity`/`serv_entity_get` (`entity.mjs`) never recursively fetches sub-decks; a fetched deck's `entity_json` is bounded to its own row + one level of children, with `parent_id` resolved only to a single display label (Track D2's FK-lookup step), never expanded into nested sibling/child deck data. D2's `format_entity_display` prompt is deliberately written to render whatever nesting depth `entity_display_data` actually contains (general instructions, not hardcoded to one level) — so once this fetch-side capability lands, the same prompt should render arbitrary-depth hierarchies without changes. Needs: (1) a way for `PGC_EntitySchema` to declare a self-referential join distinct from child-table joins, (2) recursive fetch logic in `entity.mjs` (with a depth cap — self-referential FKs can be cyclic in principle, though `PGD_Decks.parent_id` has `onDelete: CASCADE` which practically prevents cycles today), (3) `get_entity`/`list_entity`'s new steps 5-11/3-9 (Track D2) would need to walk the resulting nested structure instead of assuming one flat `children` level per entity. |
| PGC config read caching — module-level cache + TTL (generalizes Sprint 7 C1a) | No caching layer exists anywhere in the proc tier today — every `PGC_SystemContext`/`PGC_Prompt`/`PGC_TableMap` read (`chat.mjs`, `llm-harness.mjs`, `minds-eye.mjs`) is a fresh `getRows()` → SERV HTTP round-trip, cold or warm start alike (confirmed while scoping C1a). For values that tolerate a little staleness — `js_transform_timeout_ms`, a workflow's own step/prompt definitions for the duration of one run (already locked once the run starts), Novia's system prompt/preferences within one session — a per-container module-level cache with a short TTL (e.g. 60s) would cut redundant round-trips without needing event-driven invalidation: read once per cold start, reuse across warm invocations of the same container until the TTL lapses. **Must not** be applied to reads where staleness is a correctness bug — `PGC_EntitySchema`/`PGC_Schema` are deliberately read live today (see `step-executor.mjs` comment: new columns must be visible immediately without recreating the domain) and should stay that way. Needs a real design pass before building anything: (1) which specific reads are safe to cache under this rule, (2) appropriate TTL per value (tuning knobs like timeouts can tolerate longer TTLs than run-scoped data), (3) whether this belongs as one generic keyed-cache helper in `serv-client.mjs` (reusable by `chat.mjs`/`llm-harness.mjs`/`minds-eye.mjs`/`simulation-engine.mjs`/`template-resolver.mjs`) rather than a bespoke cache per file. C1a (`js_transform_timeout_ms`) is the concrete first candidate implementation once this design lands — deferred rather than building a one-off cache just for it. |
| L2c legacy path simulation (`path_results`) never gates `result.passed` | Confirmed live in run 626: `generate_workflow_paths` mislabeled 7/8 paths' `expected_terminal` (fixed separately — see `generate_workflow_paths` v4), and `path_results` correctly reported `paths_failed: 7`, but `simulation_result.passed` stayed `true` throughout — because L2c is documented as informational-only and never factors into the `passed` gate. This was flagged as an unconfirmed open question during the Sprint 7 architecture-doc audit (`docs/arch-create-workflow.md` step 30 / "Gate-bounded correction loops") and is now empirically reproduced: the step 30→31→33 "path simulation failed" repair branch is very likely unreachable in practice. Needs a design decision, not a quick fix: (1) should L2c gate `passed` at all, given how noisy/unreliable path-label matching has proven to be (labeling bugs like the one just fixed produce false failures)? (2) if it should gate, does that require hardening `generate_workflow_paths`' terminal-resolution logic first so real defects aren't drowned out by mislabeling? (3) if it should stay informational, should the step 26/27 or step 33 user-facing messages surface path_results some other way (today it's silently absorbed) rather than implying validation happened where it structurally cannot fail the gate? |
| Live step-progress notification during long workflow runs | Currently the only visible Slack activity during a run is `human_gate`/`notify`/error — a multi-step stretch between gates (e.g. several `llm_call` steps with retries) looks identical to a hang. Add a progress message posted to the reply thread on step completion, e.g. "completed step 2a (4/34) js_transform...". Should update-in-place via `chat.update` on a single progress message rather than posting once per step (avoids spamming the thread on long/fast-iterating workflows); consider gating on a minimum step count or duration so short runs stay quiet. Related: `Run/trace id missing from Slack gate acknowledgements` below — both are part of the same "what is happening right now" visibility gap. |
| Future AC: verify `design_workflow_process` acts on domain rollup-counter conventions (e.g. `PGD_Decks.card_count`) now that its memory retrieval is fixed | Sprint 7 restored `design_workflow_process`'s `memory_config` and fixed `retrieveMemories` so it now actually retrieves a domain's `schema_snapshot`/`insert_expectations` memory — which already documents, in plain language, that denormalized counters like `PGD_Decks.card_count` are "Updated via application logic after card insert/delete." No prompt change is needed for this to work; it's not a design decision to make now — `create_domain`/`design_table` proposing a rollup column and `create_workflow` designing its maintenance are both evolving-artifact behavior, correctable through the normal generation/correction loop once the LLM actually has the memory. **Action:** next time a workflow touching `PGD_Decks` (or any domain with a similar rollup column) is created or regenerated via `create_workflow`, check whether `design_workflow_process` picks up the convention from memory and designs the corresponding increment/decrement steps. If it doesn't, diagnose then (Instruction fault in `design_workflow_process`'s prompt) rather than guessing now. |
|---|---|
| Audit `inject_for` vs `{{token}}` coverage in all prompts | `embedding_config` had `inject_for` set but no `{{embedding_config}}` token in any target prompt — context was silently dropped. Audit every `PGC_SystemContext` row: for each `inject_for` entry, verify the referenced prompt contains the matching `{{key}}` token. Script: load seed_PGC_SystemContext.json + seed_PGC_Prompt.json, cross-reference, report any row where `inject_for` lists an `intent_category` whose prompt text lacks the `{{key}}` token. |
| Audit all inter-Lambda HTTP calls for missing `x-api-key` header | `slackbot/shutdown.mjs` and `proc/shutdown.mjs` both called SERV/PROC without the `INTERNAL_API_KEY` header, causing 403s. Fixed for shutdown, but other internal `fetch()` callsites may have the same gap. Audit: grep all `.mjs` files under `src/` for `fetch(` calls to `SERV_API_URL` or `PROC_API_URL` and verify each carries `'x-api-key': process.env.INTERNAL_API_KEY`. Pattern reference: `ping.mjs`. |
| Button click convention — strip actions block, preserve message content — Sprint 7 (Track: UI Polish, sub-item D) | When any interactive button is clicked, the correct pattern is to filter out only the `actions` block from `payload.message.blocks` and call `chat.update` with the remaining blocks and original `text`. This removes the stale button without replacing the message content. **Do not** call `chat.update` with new text (clobbers the message) and **do not** skip the update (leaves a stale clickable button). Applied first in `handleMindsEyeFollowupButton`. Audit `handleExplainFollowupButton` and all other button handlers to apply the same pattern — currently `handleExplainFollowupButton` replaces the full message with "💬 Follow-up submitted." which loses the original notify text. |
| Per-option gate confirmation — opt-in override to D4 content-preservation (Sprint 8, session 9) | User observed 2026-07-21 that `create_workflow`'s `21c` redundancy-review gate, after clicking **Redesign Process**, keeps the full warning + appends `✅ Redesign Process. > _<first warning line>_` — cluttered for a *regenerate* action where the warning is now stale; a cleared body + `🔄 Regenerating the process design…` reads better. **This is D4 working as designed, not a D4 flaw** — D4 (sprint-07 §515) was a deliberate 5-site audit fixing content-clobbering and correctly preserves content by default; it just offers no per-gate opt-out because no gate needed one until now. **Fix = a narrow, opt-in exception, never a default change** (so it can't regress the content-clobbering bug D4 fixed): (1) **system** — let a gate option carry optional `on_select_message` (overrides the whole `✅ …` confirmation text, incl. the `> _first line_` quote) and `clear_content` (strip the gate body, not just the buttons), rendered generically by `interactive.mjs` + threaded in the button value from `callback.mjs`; absent ⇒ today's D4 behavior. **Must not** put `create_workflow` vocabulary (e.g. "Redesign") in `/ui/slack`. (2) **domain follow-up (not backlog)** — the `21c` "Redesign Process" option in `seed_PGC_Workflow.json` sets `on_select_message`/`clear_content`. Deferred 2026-07-21 ("another day"). |
| Test/debug/deploy cadence — commit-then-deploy discipline | **Pattern:** Code fixes are committed and pushed but not immediately deployed. The user starts a new workflow run assuming the fix is live, hits the same error, and we lose a run diagnosing a problem that's already solved. Root cause: no visible signal separating "on branch" from "deployed to prod". **Fix options:** (1) Discipline rule: every code fix session ends with deploy before closing — Claude calls it out in the bye checklist if uncommitted or undeployed changes exist. (2) Lightweight deploy-status check: at session open ("hello"), compare the Lambda's last-modified timestamp against the most recent commit to detect drift. (3) Longer term: test environment so fixes can be validated without touching the user's running prod workflows. Add deploy as a hard gate to the bye checklist (alongside commit + push). |
| `create_workflow` — add "interactive session" mode option | Step 2 currently offers general modes (CRUD, report, etc.) plus a free-text "Other" that forces the user to describe a complex interaction pattern in full. Add a named **"Interactive session"** option that encodes the pattern: present a list of domain items → user picks one → system steps through a subset sequentially → user gives binary feedback (correct/not) per item without typing an answer → subset repeats until every item is mastered → advance to the next subset. This covers flashcard quiz, vocabulary drill, interview practice, and any spaced-repetition-style interaction over a domain set. Selecting it should inject a structured spec into the workflow description so `research_workflow_domain` and `generate_workflow_steps` receive a precise interaction contract rather than a free-form paragraph. Implementation: add the option to the `right_brain_modes` seed in `PGC_SystemContext` (or the step 2 `choice` gate options in `seed_PGC_Workflow.json`) with a canonical description field the LLM can reference. |
| `parse_entity_input` — two prompt defects from run 448 | **(1) Wrong empty-child rule:** LLM produces `[{},{},...]` for child tables with user-providable fields when no user data is present. Correct rule: `[]` when no data AND the child has at least one user-providable column; `[{},...]` only when ALL columns are system-managed (zero user-providable fields). Fix: rewrite the empty-child rule in the prompt to make the distinction explicit. **(2) Deck hierarchy not inferred:** "Patent set name=Spanish Vocabulary" (typo for "Parent") was interpreted as a description field rather than a self-referential parent entity reference. Full solution is Case 2/3 of the nested hierarchy backlog item. Prompt-only interim fix: add a rule recognising `parent[= :]<name>` patterns as a parent-entity reference signal and emitting `_parent_required` when the parent id is unknown. |
| `add_entity` — nested hierarchy support for self-referential FK tables | Three cases must be handled for self-referential FK tables (e.g. `PGD_Deck.parent_deck_fk → PGD_Deck.id`). **Case 1 (already works):** intra-batch parent-child rows in the same insert (`match_by: "self"` in entity schema) — handled by `entityInsertSelfRef` in `step-executor.mjs`. **Case 2 — parent exists:** user says "add a deck under Spanish Vocabulary"; `parse_entity_input` needs to emit the parent name; `add_entity` workflow needs a `serv_query` step to resolve name → id, then a `js_transform` to inject the FK before the root insert. **Case 3 — parent does not exist yet:** user wants to create both parent and child in one call. `serv_entity_insert` in `step-executor.mjs` would need to detect a top-level `parent` object in `parsedEntity`, insert it first, capture its id, then inject that id as the FK on root. **Design constraints:** (1) Atomicity gap — `serv_entity_insert` makes sequential HTTP calls with no cross-call transaction; an orphaned parent row on root-insert failure is the risk, currently acceptable. (2) `PGC_EntitySchema` does not currently model which FK column is the self-referential parent — that info is in `PGC_Schema.foreign_keys` and would need to be pre-assembled into `entitySchema` at schema-build time (in `executeServEntitySchema`). (3) `parse_entity_input` output schema must add an optional top-level `parent` object to allow the LLM to express Case 3. **Recommended implementation order:** Case 2 first (serv_query name lookup in `add_entity` workflow — no system code change), then Case 3 (extend `serv_entity_insert` + `parse_entity_input` output shape). **Extend-not-prompt principle applies:** the LLM naturally wants to express "create a deck inside another deck" — the harness should accept this rather than forcing two separate user calls. |
| **Cross-domain workflows — `create_workflow` spanning multiple domains (Sprint 7)** | The Step Processor has no single-domain constraint at runtime — a step can reference any registered table regardless of domain. All five gaps below must be addressed together. **Gap 1 — Schema load (step 1, blocking):** step 1 filters `PGC_Schema` with `domain = {{input.domain}}`. Only one domain's schema enters `domain_schema`. Every LLM call downstream (`analyze_workflow_gaps`, `design_workflow_process`, `design_workflow_dialogs`, `generate_workflow_steps`) has no knowledge of the second domain's tables. Fix: accept `input.secondary_domain`; add a second `serv_query` + `js_transform` merge step to build a labeled multi-domain `domain_schema`. **Gap 2 — `create-workflow.mjs` input contract:** `input.domain` is a single string resolved from `matchDomainAlias()`. Fix: resolve both domains before creating the WorkflowRun and inject both into input state. **Gap 3 — LLM prompts receive `domain` (singular):** `research_workflow_domain`, `analyze_workflow_gaps`, `design_workflow_process`, `design_workflow_dialogs`, `generate_workflow_steps` all carry `domain` (string) and `domain_schema` (single domain). Fix: update all five prompts to accept a labeled multi-domain schema and a `secondary_domain` hint. **Gap 4 — `PGC_Workflow.domain` registration (step 35):** registered row carries `domain: input.domain` (single value). A cross-domain workflow is invisible to domain-scoped queries and `delete_domain`. Fix options: store primary domain only (secondary noted in description) or change `domain` column to jsonb array. **Gap 5 — `delete_workflow` prompt cleanup:** `delete-workflow.mjs` deletes `PGC_Prompt` rows where `domain = workflow.domain`. For a cross-domain workflow this misses the secondary domain's prompts or deletes shared ones. Fix: extend `delete_workflow` and `delete_domain` to accept a list of domains for prompt cleanup. **Validation test vehicles:** (A) *Grocery receipt → pantry + expenses* (UC-P4 extended / W2) — fan-out write to two domains from a single receipt. (B) *Make a recipe → subtract ingredients from pantry* (UC-P5 extended) — read-then-write chain across two domains. |
| IntentMap default entry when user omits domain/type | When a user invokes a generic action ("quiz me", "start a quiz") without specifying a domain, Pass 1a matches the first registered workflow whose pattern covers the phrase. If multiple quiz-type workflows exist across domains, this silently picks the wrong one. Fix: add a `default_for` or `requires_domain` flag to `PGC_IntentMap`; when a match fires on a row marked `requires_domain` and no domain can be resolved from the input, route to a lightweight clarification gate ("Which domain would you like to quiz?") that presents the matching workflows from all domains. Implementation note: the clarification gate itself is a system-level workflow (or an early `classify-intent.mjs` path) — not per-workflow boilerplate. Prerequisite: more than one quiz-type workflow registered. |
| pgvector semantic matching for IntentMap (Pass 1.5) — supersedes phrasing-gate regex | Sprint 4 added a `text_input` gate to `create_workflow` that captures user invocation phrases and writes them as a `\|`-joined regex to `PGC_IntentMap.pattern`. This is a workable MVP but requires the user to predict their own phrasings at creation time and provides no tolerance for synonym variation. The correct long-term fix is a **Pass 1.5** pgvector semantic search: at classification time, embed the user's raw input and run a cosine similarity query against `PGC_Workflow.intent_embedding` (or a dedicated `PGC_IntentMap.embedding` column). A match above a configurable threshold routes directly to that workflow — no regex, no Tier 2 LLM call. **Prerequisites:** (1) `PGC_Workflow.intent_embedding` column populated at workflow registration (see Low Priority item below — embedding generation step in `create_workflow` step 35 area). (2) New classification pass in `classify-intent.mjs` between Pass 1c (alias match) and Pass 2 (cheap LLM): `runPass1d` — pgvector ANN query via SERV, threshold configurable via `PGC_SystemContext`. (3) `PGC_IntentMap` may need an `embedding` column if matching against IntentMap rows rather than `PGC_Workflow` directly. Once live, the phrasing gate can remain as a user-friendly labelling step but its regex output becomes optional metadata rather than the primary matching mechanism. |
| Richer episodic memory content for domain workflow runs | `distillContent` in `memory-writer.mjs` writes a generic one-liner ("Completed workflow 'quiz_flashcards' for domain 'flashcards'") for all runs — no session data, no outcomes. For workflows that produce meaningful results (quiz scores, entity counts, etc.), this is too thin to be useful in future LLM context. Fix: inspect `run.state.local_state` at distillation time and extract key outcome fields based on workflow name (e.g. for quiz_flashcards: mastered_count, total_cards, step_count → "Completed quiz_flashcards for flashcards: 13/13 cards mastered in 42 answers"). Could be deterministic (field mapping per workflow name) or a cheap sonar call for rich multi-step runs. Either way, the distilled content should be meaningful enough to inform future `create_workflow` or `fix_workflow` calls about how the workflow performs in practice. |
| `create_domain` final confirm gate — feedback text box | The last confirm gate (step 16) has approve/cancel but no way to request minor changes before committing. Add a free-text feedback input to the gate; on submission, route through a lightweight `revise_domain_schema` llm_call that applies the feedback to the current scaffold, then re-present the updated schema for a final confirmation. Keeps the lightweight "tweak before commit" path distinct from the full preference-gate re-run loop. |
| `/explain-run <run_id> <prompt>` — run-level diagnostic chat | Aggregates step outputs across an entire `PGC_WorkflowRun` and seeds a chat session with that context (see `docs/arch-session.md` §6.3). **G3 dependency:** G3 writes a `PGC_Memory` row with `source_run_id = run.id` for every qualifying domain run. `/explain-run` queries `PGC_Memory WHERE source_run_id = <run_id>` to get the pre-distilled episodic anchor, then builds on it with the full `PGC_WorkflowRunStep` output sequence. Without G3, `/explain-run` must reconstruct run context from raw step records with no distilled narrative. New SQS type `EXPLAIN_RUN`, new intent category `explain_run`, and handler additions to `classify-intent-tiers.mjs` + `handler.mjs`. Full schema slot already reserved in `PGC_Session`. |
| Table maintenance — PGC_Memory TTL cleanup | Nightly job deletes PGC_Memory rows WHERE `expires_at < NOW()`. Episodic memories are written with `expires_at = NOW() + INTERVAL '90 days'`. Semantic and procedural rows have `expires_at = NULL` and are not touched. Implement as an EventBridge Scheduler → PROC `maintenance` endpoint → SERV deleteRows. Configurable via `PGC_SystemContext` key `episodic_ttl_days` (default 90). |
| Table maintenance — PGC_WorkflowRun archival | Completed/failed/cancelled runs older than 90 days are moved to a `PGC_WorkflowRunArchive` table (same schema). Active table stays small; stats view performance stays predictable. Nightly EventBridge → PROC maintenance. Child `PGC_WorkflowRunStep` rows must be moved first (FK order). Only terminal-status runs are eligible — never archive `running` or `awaiting_human_gate` rows. |
| Table maintenance — PGC_WorkflowRunStep archival | Cascades with `PGC_WorkflowRun` archival. `PGC_WorkflowRunStep` is the idempotency log — only rows whose parent run is in a terminal state are eligible. Move to `PGC_WorkflowRunStepArchive` in the same transaction as the parent run archival. Add `ON DELETE CASCADE` to FK if not already present so future hard-deletes clean up automatically. |
| Memory consolidation — semantic deduplication | When multiple semantic memories accumulate for the same domain+topic, a weekly job consolidates them: query memories older than 30 days with scope `{"domain":X}` and tags containing `schema`, run a cheap sonar call to distil into one record, delete originals. Consolidated record gets `priority 2`. Prevents unbounded corpus growth for active domains. |
| `preview_gate` — Novia Phase 2 read tool for human_gate rendering validation | A `renderGatePayload(step, localState)` utility in `ui/slack` that resolves `message_template` tokens and builds the Block Kit payload from a step definition + a real or synthetic `local_state` snapshot. L1 simulation validates gate structure but cannot verify that template tokens resolve correctly against real data — a token that evaluates to null or the wrong shape only surfaces at runtime. Exposed as a Novia read tool (`preview_gate`) so Novia can validate gate rendering during diagnostic sessions before proposing workflow fixes. Also useful as a standalone dev utility when authoring human_gate steps. **Prerequisite:** Novia Phase 1 (N2 read tools) complete. |
| Novia Mode 4 /chat agent — agentic companion | Upgrade `/chat` from Mode 2 (managed history) to Mode 4 (agentic tool use loop). LLM can call tools: `get_workflow`, `analyze_workflow`, `propose_workflow_fix`, `apply_workflow_fix`, `serv_query`, `write_memory`, `add_system_context`, etc. Tool definitions in `PGC_Capability`. Write tools require human gate before execution. Persona (Novia/Javier) stored in `PGC_Memory` scope `{"topic":"persona"}` — global, no user scoping needed (single-user instance). See `docs/arch-memory.md` Section 9 for full design. |
| LLM model alias system | Add `llm_model_aliases` jsonb entry to `PGC_SystemContext`. `PGC_Prompt.model` stores aliases (`smart`, `cheap`, `fast`) or pinned literal model IDs. Harness resolves aliases at call time. One `PGC_SystemContext` update upgrades all alias-using prompts when a new model ships. Add `dev_scripts/audit-model-ids.mjs` to find all pinned literal IDs. See `docs/arch-memory.md` Section 11. |
| EventBridge Scheduler for scheduled workflows | User-created scheduled runs stored in new `PGC_Schedule` table. `manage_schedule` workflow creates/deletes EventBridge Scheduler rules. `LambdaExecutionRole` needs `scheduler:CreateSchedule`, `scheduler:DeleteSchedule`, `scheduler:GetSchedule` permissions. EventBridge → ProcFunction Lambda directly (not via API Gateway). See `docs/arch-memory.md` Section 12. |
| PGC_Memory table — create and register | DDL with jsonb scope, GIN indexes, token_estimate, priority, expires_at. Register in PGC_Schema + PGC_TableMap (`allow_insert: true`, `allow_delete: true`). First step in memory layer implementation sequence. |
| Deduplicate shared prompt instruction blocks into PGC_SystemContext | `generate_workflow_steps` and `fix_workflow_routing` maintain parallel copies of CRITICAL ROUTING RULES and REPAIR RULES. Drift between them caused the condition routing bug in Sprint 2 (fix_workflow_routing Rule 6 contradicted generate_workflow_steps Rule 2 for months). Move shared blocks (routing rules, condition format contract, loop back-edge rules) into named `PGC_SystemContext` keys and inject via `inject_for`. One update fixes all consuming prompts. |
| Domain schema awareness across llm_call steps in create_workflow | `research_workflow_domain` explores domain schema and user preferences but its findings are discarded when the LLM call ends — `generate_workflow_steps` receives no schema context. Fix: add a `serv_query` step before `generate_workflow_steps` that fetches `PGC_Schema` rows for the target domain and injects them as `domain_schema`. No new step types required. Also applies to `fix_workflow_routing` — the repair agent currently has no visibility into the domain tables the workflow reads from. |
| /chat session context — inject environment snapshot | `/chat` answers in a vacuum: it has no knowledge of the user's domains, registered workflows, or domain data. Enrich the system message with a real-time snapshot: `PGC_DomainHelp` rows (what domains exist, their aliases and commands), `PGC_Workflow` rows for those domains (workflow names, descriptions, intent keywords), and optionally recent `PGC_WorkflowRun` activity (status, last-run timestamp). Implement as a `serv_query` step before the first `llm_call` in the chat flow, storing the snapshot in `local_state` and resolving it into the prompt via `{{template}}` injection. |
| `create_domain_example` system context is stale (pre-Sprint-2) | `PGC_SystemContext.create_domain_example` describes the v1 12-step linear workflow — it does not include the Sprint 2 additions (duplicate pre-check 0a–0d, right-brain research step 1R, preference gate iterator 1a–1c, updated left-brain call). Generation and troubleshooting LLMs that receive this context see the old pattern and have no reference for the L/R brain structure. Fix: rewrite `create_domain_example` to reflect the v9 data flow. |
| `PGC_Prompt.input_probe` column | Add `input_probe jsonb nullable` to `PGC_Prompt`. Steps: (1) `POST /api/v1/serv/table/addColumn` with `tableName: "PGC_Prompt", columnName: "input_probe", type: "jsonb", nullable: true` (also registers the column in `PGC_Schema`). (2) Add an `input_probe` object to each row in `seed_PGC_Prompt.json` — the canonical test input exercising that prompt's required variables. (3) Run `node dev_scripts/upsert-prompt.mjs`. Why: gives simulation, `/troubleshoot`, and dev scripts a stable probe input per prompt without constructing one ad-hoc; enables automated prompt regression testing. |
| `domain: null` on `create_workflow` runs | `input.domain` is null throughout — intent preprocessor passes only `userInput`. Fix: resolve domain before CREATE_WORKFLOW SQS dispatch and inject `domain_schema` into `research_workflow_domain` input |
| `research_workflow_domain` receives no domain schema | Prompt only receives `workflow_description` and `domain` (the latter is null). Without schema context the right brain cannot surface domain-specific preference questions. Fix: add `domain_schema` as an input variable |
| `fix_workflow_steps` prompt text says "complete array" | Prompt still instructs LLM to return the full corrected step array. Should say "return only the steps you changed". Reduces output tokens, eliminates risk of unrequested steps |
| `createTable` DDL + PGC_Schema insert not in a transaction | Physical table can exist without registry row on partial failure |
| `updateTable` ALTER TABLE | Currently metadata only — does not execute ALTER TABLE |
| `iterator` cannot express multi-step per-item sequences | Requires `sub_workflow` step type (MVP) or flat loop pattern (Option B) |
| `sub_workflow` step type — create_domain add-table migration | Option B (text_input gate + inline LLM) should be replaced with Option C: a reusable `design_table` sub-workflow. Prerequisite: `sub_workflow` step type live |
| `serv_aggregate` step type | GROUP BY + aggregation at DB level. Required for budget reports, portfolio summaries. Alternative to `llm_call` for arithmetic over query results |
| `createView` SERV endpoint — database views for pre-aggregated queries | Add `createView` case to `schema.mjs` alongside `createTable`. Accepts `{ viewName, selectSql, target }`, runs `CREATE OR REPLACE VIEW "${viewName}" AS ${selectSql}`, and registers the view in `PGC_Schema` (target: pgd, type: view) and `PGC_TableMap` (allow_read: true) so existing `serv_getRows` can query it without any step type changes. MVP for W4 (Sprint 6) uses a manually-created view; this makes view creation available to Novia and `create_domain` as a first-class operation. Replaces the need for `serv_aggregate` for fixed aggregation patterns (monthly expense totals by category, pantry reorder candidates, portfolio summary). `serv_aggregate` remains useful for ad-hoc parameterised aggregations the view cannot express. |
| SERV-Query cross-entity parameterised SELECT | Join across multiple PGD tables with pagination. Required for complex entity reports |
| ~~Gate types: `select_one`, `select_many`~~ | ✅ **Superseded (Sprint 7, Track K).** Resolved by the `form` gate type rather than by finishing these. A widget is a *field type*, not a gate type — `form` collects any number of typed values (text, select, multi_select, radio, checkbox, date, time, datetime) in one gate, so `select_one`/`select_many`/`date_input` never need to exist. The dead `buildDialog()` stubs were removed: they emitted `select`/`checkbox` dialog fields that `callback.mjs` had no case for (only `radio` rendered), their value never reached `output_key`, and they were absent from every gate_type enum — unreachable, and a trap for anyone who found them. |
| `PGC_Workflow.intent_embedding` population at domain creation | Add embedding generation step to `create_domain` workflow and `generate_crud_workflows` prompt. Prerequisite for pgvector Pass 2 semantic search |
| `PGC_WorkflowRun.session_id` FK column | Add `session_id integer FK → PGC_Session.id nullable` to `PGC_WorkflowRun`. Migration script needed — column did not exist at bootstrap |
| Live prompt export back to seed files | When the right-brain improves a prompt, the improvement lives only in DB. Fix: `dev_scripts/export-prompts.mjs` reads live rows and overwrites `seed_PGC_Prompt.json`. Required before right-brain improvement loop is useful at scale |
| Dependency injection for DB clients | Needed for unit testability — clients currently instantiated at module level |
| `add_<domain>` workflows already in DB from v2/v3 are thin stubs | Existing domains (e.g. recipes) have the old 2-step workflow. Delete and recreate domain to get the v4 LLM-parse-first workflow, or manually upsert via `upsert-workflow.mjs` |
| `init-brain.mjs` shared DDL utilities | `buildCreateTableSQL` and `getClient` imported by `schema.mjs` from `init-brain.mjs`. Refactor: extract to `src/shared/serv-utils.mjs` |
| `PGC_Schema` not updated when `ALTER TABLE` adds a column | Every `ALTER TABLE` on a PGC table must be paired with an `UPDATE PGC_Schema SET columns = columns \|\| '[{"name":...}]'` |
| Implement remaining Block Elements | Create-workflow LLM design-workflow-dialogues need a complete menu of widgets to handle any dialog requirement like date-picker, URL links, etc. See Block Elements table in slack-block-kit.md |
| Extend human dialog rendering to use `markdown` block type — Sprint 7 (Track: UI Polish) | `dialogToBlocks()` in `callback.mjs` currently uses `mrkdwn` for all text fields (`typography`, `description_list`). The `markdown` block type (`{ type: 'markdown', text }`) renders standard markdown — `**bold**`, `~~strikethrough~~`, fenced code blocks, tables — which is what LLMs naturally produce. Extend `typography` and `description_list` fields to emit `markdown` blocks, and update the `design_workflow_dialogs` prompt to document the available formatting syntax so `create_workflow` can generate richer gate dialogs. See `docs/slack-block-kit.md` Markdown block section. |
| `memory-client.mjs` — push scope/tag filters to DB via `jsonb_contains` | `retrieveMemories` currently calls `getRows('PGC_Memory')` with no filters and applies scope, expiry, and tag checks client-side. Now that SERV supports the `jsonb_contains` (`@>`) op, scope and tag filters can be pushed to the DB — eliminating a full-table scan on every LLM call. Client-side post-filter can be kept as a safety net. Also: the greedy selector at line 190 uses `break` on first oversized row — a single high-priority fat memory starves all smaller ones below it; change to `continue` to skip-and-keep-going. **Context:** surface this item whenever `src/proc/memory-client.mjs` is touched. |

### Low Priority

| Item | Notes |
|---|---|
| Deferred / on-demand tool loading for Novia — **measured and rejected at current catalog size** | The idea: split the tool catalog into sets and give Novia a `load_toolset` tool so only the relevant schemas sit in context. **Measured Sprint 10, 2a:** 23 tools cost **3,162 tokens** (~137/tool), and the block **caches** — ~$0.00095/turn read, against a measured $0.019 cached turn, so the whole catalog is ~5% of a turn's input cost. The reason it loses is the invalidation hierarchy: **tool definitions render at position 0**, so adding or removing one mid-round invalidates the tools cache, the system cache *and* the entire messages cache. A `load_toolset` call at turn 10 of a 30k-token round pays ~$0.1125 to re-create the transcript in order to save $0.00095/turn — **break-even is ~110 further turns**, against a round limit of 8, and the call itself burns a turn (the scarce resource under the 240s Lambda ceiling). **Revisit when:** the catalog passes ~100 tools, rounds get much longer, or the gateway exposes Anthropic-native **tool search** with `defer_loading`, which *appends* schemas instead of swapping them and so preserves the prefix — unprobed on this gateway. If routing quality (not cost) is ever the motivation, the lever is better tool descriptions; 2a found routing sound at 23 choices. |
| L1 simulator: cross-step type inference for local_state variables | `simulate_workflow` checks each step in isolation and cannot trace the type of a `local_state` variable from its initialization step to its use step. This produces false-positive warnings on `js_transform` expressions that call array methods (`.indexOf`, `.filter`, `.map`) on variables that are reliably array-initialized in upstream steps (e.g. `mastered_card_ids: []` in step 1, used in step 7). Fix: for each `local_state` variable referenced in an expression, walk backwards through the reachable step graph to find the most recent write (via `output_key`) and infer the type from the writing expression's return shape. If every path to the current step passes through an array-initializing write, suppress the method-call warning. Significant complexity — lower priority than other L1 gaps, but produces noise that makes real warnings harder to spot. |
| `openapi.yaml` sync audit — remove stale ping endpoints and review full spec | `openapi.yaml` is likely out of sync with current routes. Known gap: `/ping` endpoint variants (`api`, `sqs`, `llm`, `e2e`, `db`, `core`) may have stale or missing entries. Full audit needed: (1) remove or update any ping-related paths that no longer match `src/ui/slackbot/ping.mjs`; (2) compare every route in `handler.mjs` (slackbot + proc) against `openapi.yaml` paths and flag missing or misdescribed entries; (3) add any Sprint 5 endpoints (`/novia`, `/chat`, `/explain`, `MINDS_EYE` SQS types) before implementation per spec-first convention. |
| Stale buttons persist intermittently after gate selection | When a user selects a preference in an iterator human_gate, the `chat.update` call that replaces the buttons occasionally does not arrive before the next gate posts, leaving the previous gate's buttons visible. Intermittent. Likely a Slack API race between `chat.update` and `chat.postMessage` — investigate whether the update should be fire-and-forget before resuming the gate or whether a retry/ack pattern is needed in `callback.mjs`. |
| Test environment (master) | Stand up a parallel AWS environment (separate SAM stack, separate RDS instance, separate Slack workspace) so changes can be validated end-to-end before touching prod. Needed before any concurrent contributors or automated integration test runs against live infra. **Prerequisites (do these first, all in this table):** *Declarative indexes in PGC table templates* (reproducibility from templates alone), *`README.md` environment bootstrap coverage* (the single spin-up reference), *Remove hardcoded prod URLs from dev-script code* (so seed tooling doesn't default to prod). The **LLM replay harness (Sprint 8)** is the enabler that makes a non-prod env affordable to exercise — replaying a recorded corpus costs $0, so a test env can be driven without burning Perplexity quota. |
| Declarative indexes in PGC table templates (test-env prerequisite) | **Design impl gap.** PGC table templates (`src/serv/templates/pgc/*.json`) cannot declare a non-unique index — `buildCreateTableSQL` (`init-brain.mjs`) reads only `type`/`primaryKey`/`nullable`/`unique`/`default` per column plus `constraints`/`foreign_keys`/`triggers`. Every non-unique index in the system lives in a bespoke imperative function (`installPGCMemoryIndexes`, and — added interim in Sprint 8 A1 — `installPGCSessionIndexes` for `idx_pgc_session_fingerprint_hash`), so a table's JSON is **not self-describing**: a reader cannot see which indexes exist, and a fresh bootstrap depends on remembering to call each function. This blocks a clean **test environment**, whose whole value is reproducibility from the templates alone. **Fix:** add an optional `indexes` array to the template shape (`{ name, columns, method?, where? }` — `method` for GIN, `where` for partial), teach `buildCreateTableSQL` to emit `CREATE INDEX IF NOT EXISTS` generically, then migrate PGC_Memory's four indexes and A1's `idx_pgc_session_fingerprint_hash` into their templates' `indexes` arrays and delete the bespoke functions. **Decision to make first:** `buildCreateTableSQL` is also the *runtime* domain-table builder (LLM payloads flow through it), so decide whether `indexes` is honored at runtime or stripped to bootstrap-only before exposing it. Do this as part of building the test env, not before. |
| `README.md` environment bootstrap coverage | README currently describes what the system does but not how to create a new environment from scratch. Add a "Bootstrapping a new environment" section covering: AWS prerequisites, SSM parameter names and values, `sam build && sam deploy`, `POST /api/v1/serv/bootstrap`, and the `dev_scripts/upsert-*.mjs` seed sequence. Should be the single reference for spinning up prod or test. |
| Remove hardcoded prod URLs from dev-script code (test-env prerequisite) | **The wart.** The prod API base `https://enwwi5aulf.execute-api.us-east-2.amazonaws.com/Prod` is hardcoded in dev-script header comments as the usage example — `dev_scripts/upsert-step-type.mjs`, `upsert-workflow.mjs`, `upsert-system-context.mjs` (`set SERV_API_URL=https://enwwi5aulf... && node ...`). The executable code is already env-agnostic (every dev script reads `process.env.SERV_API_URL`), so this is comment-only — but load-bearing for a test env: the examples quietly teach "point at prod," so anyone following them **seeds prod while trying to seed test**, silently. **Fix:** replace the literal in every dev-script usage comment with a neutral placeholder (`SERV_API_URL=<your-env-api-base>`) or drop the URL entirely and point at the one bootstrap reference. Same literal is scattered across seven docs (`arch-data.md`, `architecture.md`, `openapi.yaml`, `unit-test-setup.md`, `arch-replay.md`, `README.md`, `CLAUDE.md`) — audit those in the same pass; the AWS base belongs in **one** place (the README "Bootstrapping a new environment" section), referenced elsewhere, never copied. **Part of the test-env cluster** — do it alongside *Test environment* (the master item), *Declarative indexes in PGC table templates (test-env prerequisite)*, and *`README.md` environment bootstrap coverage*; a test env is worthless if the tooling that seeds it defaults to prod. |
| `design-domain.mjs` dead code — remove in Sprint 8 with `/chat` cleanup | Pre-Step-Processor handler for `DESIGN_DOMAIN` SQS messages and `POST /proc/design-domain` HTTP route. Confirmed dead: nothing in the codebase enqueues `DESIGN_DOMAIN`; `slackbot/create-domain.mjs` enqueues `CREATE_DOMAIN` which routes to `proc/create-domain.mjs` instead. Remove: (1) `src/proc/design-domain.mjs`; (2) `import` + `case 'design-domain'` in `proc/handler.mjs`; (3) `DESIGN_DOMAIN` comment block in `proc/handler.mjs`; (4) the stale comment in `slackbot/callback.mjs` line 307 referencing `design-domain.mjs`; (5) `/proc/design-domain` entry in `openapi.yaml` if present. Bundle with `/chat` dead code removal — same sprint, same PR. |
| `generate_crud_workflows` — orphaned prompt seed (obviated) | Confirmed dead (2026-07-23): `generate_crud_workflows` is a step in **no** workflow (0 references in `create_domain`) and called by **no** `src/` code — only its own `seed_PGC_Prompt.json` row (v9) and docs remain. Obviated by the generic `list_entity`/`get_entity`/`add_entity`/`update_entity` workflows, which handle CRUD across all domains without generating per-domain workflows. **Cleanup:** remove the `generate_crud_workflows` row from `seed_PGC_Prompt.json` (and re-run `upsert-prompt.mjs` to drop it from the DB, or leave the DB row as harmless-dead), and scrub stale references in `docs/arch-prompt-rules.md`. Low-risk — nothing invokes it. Check `PGC_IntentMap` for any phrase still routing to it before deleting. |
| `/chat` removal — **decision pending, not yet approved** (moved out of Sprint 8, 2026-07-23) | `/chat` is believed obviated by Novia, but the deletion is **not yet decided** — the user has not confirmed they want it gone. **Do not remove until confirmed.** If approved, the recipe: remove `ui/slackbot/chat.mjs`, `proc/chat.mjs`, the `CHAT_MESSAGE` SQS type, the route, and the `openapi.yaml` entry; update `docs/architecture.md` (message-type table, directory structure) and `docs/arch-session.md`. **Guardrail:** `/explain`, Novia, **and the replay corpus** all read `PGC_Session`/`PGC_SessionEntry` — those tables must stay untouched; only the chat-specific paths go. Bundle with the `design-domain.mjs` dead-code removal above (same PR). **Decision to make first:** confirm nothing (a user habit, a fallback path, a test) still depends on `/chat` before deleting. |
| Orphan table cleanup tooling | Failed partial runs leave orphan tables — `delete-domain` covers full domains; per-table cleanup is manual |
| AWS infrastructure cost — Bastion Host public IPv4 | EC2 Bastion accrues ~$2.82/month. Replace with AWS SSM Session Manager when promotional credits near exhaustion |
| W3C `traceparent` format for `traceId` | Adopt `{version}-{traceId}-{parentId}-{flags}` when observability tooling added |
| Option B name-based delete/update | Allow `/m delete recipes SWEET POTATO` to find by name then confirm by resolved id. Requires `serv_entity_query` pre-step |
| `update_entity` missing field values instructive error | `/m update recipes id=3` with no field=value pairs should post instructive error without creating a WorkflowRun |
| Run/trace id missing from Slack gate acknowledgements | Human gate dialogs do not surface `workflowRunId` or `traceId` — impossible to correlate with CloudWatch without querying DB |
| `generate_crud_workflows` prompt description length | `PGC_DomainHelp.description` used in help button labels. Add prompt rule: description ≤ 50 chars |
| `add_entity` child iterator timeout ceiling | Sequential iterator bounded by Lambda timeout. At 60s / ~400ms per insert, safe ceiling ~120 child rows. Document in runbook |
| Integration tests | Defer until intent pipeline complete — use `testcontainers` + PostgreSQL |
| CI/CD GitHub Actions | Deliberately deferred until `template.yaml` stabilises |
| `callback` routing pattern not enforced at compile time | Every PROC endpoint must use `req.callback ?? req.body?.callback ?? null`. Currently convention only |
| Terraform state — legacy infrastructure | Terraform config in `terraform-aws/` predates SAM migration. Check for orphaned AWS resources before decommissioning |
| Azure MSAL token utility (`src/lib/getAccessToken.js`) | Vercel-era artifact. Assess for Teams Experience tier or decommission |
| `upsert-workflow.mjs` required on fresh deploys | `init-brain` uses `ON CONFLICT DO NOTHING` — must run `upsert-workflow.mjs <name>` after any workflow step changes |
| `output_key` on non-`text_input` gates is misleading | `review_object` and `confirm` gates do not write to `local_state[output_key]` on confirm |
| `PGC_SystemContext.step_type_contracts` can become stale | Re-run `seed_PGC_StepType.mjs` then `seed_PGC_SystemContext.mjs` when a new step type goes live |
| `toEntityName()` in `classify-intent.mjs` is dead code (fallback only) | Remove once all domains are recreated with `domain` column populated |
| `orderBy` field in entity queries not driven by `PGC_EntitySchema` | Add optional `display_order_column` to `PGC_EntitySchema` — `list_entity` reads it when present |
| `formatRecordList` renders id-only for tables where label column is not `name` | Add `display_column` hint in `PGC_TableMap` |
| `parse_entity_input` generic prompt — domain-specific refinement | For domains where column semantics are non-obvious, parse quality degrades. Fix: `PGC_Prompt.error_log` + right-brain refinement |
| `generate_crud_workflows` v2 `input_variables` stale | Seed row still lists `domain_help` as required; create_domain step 6 no longer passes it. Renders as empty string — not breaking |
| CHECK constraint `output_schema` validation | Tighten schema to require `expression` and disallow `columns` on check type constraints |
| `output_key` on `review_object` gate should warn if set | Executor has no guard; only `text_input` gates write to `local_state[output_key]` |
| Session context window size configurable | `chat_defaults` key in `PGC_SystemContext` should define `session_context_limit` (default 20). Currently not implemented |
| Alias management workflow `/mind edit aliases for <domain>` | View and update `PGC_DomainHelp.aliases` from Slack without touching the DB |
| Pass 2 keyword scan excludes `domain: null` workflows | System workflows unreachable via Pass 2 — causes unnecessary Tier 2 sonar LLM calls |
| `list_recipes` notify shows "Found recipes record(s)" without count | `{{results.length}}` not resolving on one run. Right-brain fix — prompt variance. Do not patch template resolver |

---

## 2. Tangential Features

Features designed but deferred — require the Step Processor to exist first, or represent meaningful scope expansion.

### 2.0 Pre-Sprint 5 Design Session — Primitive Taxonomy

**Do this during Sprint 5 planning before scoping any agentic loop work.**

Define clear boundaries between: **workflow**, **workflow step**, **skill**, and **tool**. These terms are currently used informally and the distinctions will matter once agents can compose capabilities dynamically. The agentic loop (Novia, Sprint 5) requires the taxonomy to be decided first — otherwise the architecture will drift as soon as agents start generating or selecting their own execution paths.

Questions to resolve:
- What makes something a workflow vs a single step? (atomicity boundary, SQS envelope, human gate eligibility)
- What is a skill? Is it a named workflow, a named capability in `PGC_Capability`, or something else entirely?
- What is a tool? Is it a `capability_call` step, an MCP-style callable, or a pre-built `serv_*` step type?
- Which of these can an agentic loop generate at runtime vs which must be pre-registered?
- How does the static/evolving artifact boundary apply to each primitive?

Output: a 1-page decision record added to `docs/architecture.md` before any Sprint 5 implementation starts.

### 2.1 External API Registry — capability_call Step Type

#### The problem

`js_transform` is restricted to pure synchronous data transformations. External data enrichment from third-party APIs cannot be done safely in LLM-generated JS:
- `vm.runInNewContext` timeout does not apply to async operations
- LLM-generated fetch calls are an exfiltration vector — a prompt injection attack or hallucinated URL could send workflow state to an attacker's endpoint
- API keys embedded in generated code are exposed in `PGC_Workflow` rows
- No rate limiting, retry logic, or circuit breaking on arbitrary fetch

#### The design

The system maintains a **capability registry** of approved external integrations. Each registered capability defines what can be called, how to authenticate, and what parameters are allowed. The LLM generates workflow steps that reference capability keys — it never constructs URLs, never sees API keys, and cannot call anything outside the registry.

**PGC_Capability schema extension:**

| Column | Type | Notes |
|---|---|---|
| base_url | text | Root URL for the API |
| endpoints | jsonb | Named endpoint templates |
| auth | jsonb | Auth config — `{ type: "query_param", key: "token", ssm_path: "..." }` |
| allowed_params | jsonb | Whitelist of parameter names the LLM may supply |
| rate_limit | text | Human-readable limit — e.g. "60/minute" |
| timeout_ms | integer | Per-call timeout. Default 5000ms |

Auth credentials are stored in SSM, never in the database.

> **SUPERSEDED IN PART — 2026-08-11. The table shipped with a different shape and this
> section has not been rewritten to match; read `docs/arch-data.md` for the live contract.**
>
> The two models differ in their unit, not only their columns. This design is **one row per
> provider** with many named endpoints; what shipped is **one row per operation**:
> `endpoint` (a full URL), `method`, `auth_ref` (an SSM parameter name), `input_schema`,
> `output_schema`, and `category: 'external'` — not `'external_api'`, which the CHECK
> constraint rejects. So the Finnhub example below is three live rows (`finnhub_quote`,
> `finnhub_candles`, `finnhub_company_profile`), not one.
>
> **What the shipped shape does not carry, and someone building `capability_call` must settle
> first:** `allowed_params` (the parameter whitelist — `input_schema` describes the shape but
> is not enforced as a boundary), `rate_limit`, and `timeout_ms`. All three currently live in
> `notes` as prose, which no code reads. The exfiltration argument above is the reason
> `allowed_params` existed, so it is the one that matters: per-operation rows make the URL
> fixed rather than templated, which removes part of that risk, but not the parameter-injection
> half.
>
> Steps 1 and 5 of "What needs to be built" are done. Steps 2, 3, 4 and 6 are not.

**New step type: capability_call**

```json
{
  "step": "3",
  "type": "capability_call",
  "capability_key": "finnhub_quote",
  "endpoint": "quote",
  "params": { "symbol": "{{state.ticker}}" },
  "output_key": "current_price",
  "on_success": "next",
  "on_else": "cancel"
}
```

#### Finnhub integration — first planned capability

```json
{
  "capability_key": "finnhub",
  "category": "external_api",
  "description": "Finnhub stock market data — quotes, candles, company profiles",
  "status": "planned",
  "base_url": "https://finnhub.io/api/v1",
  "endpoints": {
    "quote":        "/quote?symbol={{symbol}}",
    "candles":      "/stock/candle?symbol={{symbol}}&resolution={{resolution}}&from={{from}}&to={{to}}",
    "company_info": "/stock/profile2?symbol={{symbol}}"
  },
  "auth": {
    "type": "query_param",
    "key": "token",
    "ssm_path": "/evolving-mind-ai/finnhub-api-key"
  },
  "allowed_params": ["symbol", "resolution", "from", "to"],
  "rate_limit": "60/minute",
  "timeout_ms": 5000
}
```

#### What needs to be built

1. PGC_Capability schema extension — add the API Registry columns listed above
2. SSM parameter for Finnhub API key
3. New `capability_call` row in `PGC_StepType` seed data
4. Step Processor handler for `capability_call`
5. Finnhub seed row in `PGC_Capability`
6. Rate limiting — token bucket in `PGC_WorkflowRun` state or a dedicated table

### 2.2 js_transform Safety Analysis — Synchronous Constraint

`vm.runInNewContext({ timeout: N })` in Node.js reliably kills synchronous infinite loops. It does NOT apply to async operations. The chosen approach — prohibit async in `js_transform`, use `capability_call` for I/O — is correct for this system. External data enrichment is a `capability_call` concern. The distinction between "transform data I already have" and "fetch data I don't have" is architecturally meaningful and enforced.

---

## 3. Build History

Session tags and what was completed. Authoritative source is `git log --oneline`.

| Tag | What was completed |
|---|---|
| `v3.2-scaffolding-complete` | All 5 pings pass (ping-api, ping-llm, ping-sqs, ping-db, ping-e2e) |
| `v3.2-ping-complete` | ping-sqs threading fixed, ping-e2e full round trip with RDS version string |
| `v3.2-serv-schema-complete` | SERV-Schema all CRUD endpoints, init-brain bootstrap, 4 PGC system tables |
| `v3.2-pgc-workflow-tables-complete` | 10 PGC system tables bootstrapped and seeded |
| `v3.2-callback-abstraction-complete` | Generic callback object, SYSSQSCallbackResults queue rename |
| `v3.2-serv-table-partial` | SERV-Table getRows + insertRow, wired into serv handler |
| `v3.2-create-domain-scaffold` | /create-domain end to end with hardcoded recipes scaffold |
| `v3.2-create-domain-live-llm` | /create-domain live LLM via Perplexity Agent API + json_schema output |
| `v3.2-r14-r15-complete` | FK/constraint normalisation moved to SERV layer; response_format restored on LLM call |
| `v3.2-slack-signing-complete` | Slack signing secret verification added to SlackbotFunction handler |
| `v3.2-template-cleanup` | SchemaQueue + DLQ removed, LambdaInvokePolicy removed, stale env vars cleaned |
| `v3.2-clean-baseline` | All pings passing, Lambda invoke pattern fully gone, clean foundation for Phase 2 |
| `v3.2-interactive-complete` | /interactive endpoint live, /help command proves full interactive loop end-to-end |
| `v3.2-serv-table-complete` | SERV-Table updateRows + deleteRows complete. openapi.yaml v3.3.2 |
| `v3.2-shutdown-complete` | /shutdown command — SlackbotFunction + ProcFunction, ephemeral Slack response |
| `v3.2-serv-entity-complete` | SERV-Entity all six routes complete. PGC_EntitySchema upsert_key added. openapi.yaml v3.3.3 |
| `v3.2-bootstrap-clean` | init-brain installs set_updated_at() on PGD. seed_PGC_Schema upsert_key synced |
| `v3.2-design-domain-foundation` | shared/llm-client + shared/serv-client extracted. proc/review-output. proc/design-domain first pass. openapi.yaml v3.3.4 |
| `v3.2-design-domain-e2e` | callback routing fix. callback.mjs DESIGN_DOMAIN_RESULT + ERROR handlers. Full Slack flow confirmed |
| `v3.2-refactor-complete` | Phase 1 refactoring closed out. All pings passing |
| `v3.2-design-domain-gate-complete` | proc/design-domain Block Kit review gate + in-place remove. human_gate suspend/resume wired |
| `v3.2-step-processor-complete` | Step Processor fully operational. First successful create_domain end-to-end (WorkflowRun 12) |
| `v3.2-tangential-features` | /create-domain + /help fully wired to Step Processor. dev_scripts/upsert-workflow.mjs |
| `v3.2-intent-preprocessor-complete` | Intent Preprocessor fully operational. mind.mjs + classify-intent.mjs + classify-intent-tiers.mjs |
| `v3.2-crud-adhoc-complete` | Ad_hoc CRUD execution from /mind fully operational. All four CRUD verbs working |
| `v3.2-create-domain-with-crud` | First complete create_domain end-to-end: 5 human gates, 4 PGD tables, CRUD registered |
| `v3.2-create-workflow-complete` | create_workflow fully implemented. on_else human_feedback. simulate step type. Pass 2b routing rules |
| `v3.2-create-domain-complete-w-help` | Gap 4 (entity schema) + Gap 1 (interactive help). create_domain v5 (17 steps). help workflow v2 |
| `v3.2-gap3-add-workflow` | Gap 3 rich ingestion. parse_entity_input v1. generate_crud_workflows v4. executeTop completed guard |
| `v3.2-generic-crud-complete` | Generic *_entity workflows. create_domain step 9 inserts IntentMap directly. Recipes full CRUD |
| `v3.2-intent-preprocessor-phase-b-complete` | Phase B pre-pass + 50 unit tests passing |
| `v3.2-js-transform-sandbox-serv-entity-schema` | condition step type. js_transform generic expression sandbox (acorn + vm). serv_entity_schema |
| `v3.2-option-c-domain-registration` | Deterministic domain registration |
| `v3.2-local-state-sandbox-builtins-removed` | local_state in js_transform sandbox. All transform_type built-ins replaced by self-contained expressions |
| `v3.2-troubleshoot-fix-workflow-complete` | Tier 1 reactive self-repair loop. troubleshoot-workflow.mjs + fix-workflow.mjs |
| `v3.2-response-format-max-tokens` | response_format restored on Perplexity. max_output_tokens per-prompt. diagnose-prompt-schema.mjs Tier 1b |
| `v3.2-session24-complete` | Iterator gate resume fix. diagnose_prompt_schema R1–R6 validated. pgvector promoted to Active |
| `v3.2-session25-complete` | isSonar guard in llm-client. fence extraction regex. diagnose_prompt_schema v4. PGC_Prompt probe_input + max_output_tokens |
