{
  "table_name": "PGC_DomainHelp",
  "target": "pgc",
  "description": "User-facing command aliases and help text per domain. Populated at runtime when a domain is created. Powers /help {domain} responses and semantic domain resolution.",
  "columns": [
    { "name": "id",          "type": "serial",      "primaryKey": true },
    { "name": "domain",      "type": "text",        "nullable": false, "unique": true },
    { "name": "aliases",     "type": "jsonb",       "nullable": false, "default": "'[]'::jsonb" },
    { "name": "description", "type": "text",        "nullable": true },
    { "name": "commands",    "type": "jsonb",       "nullable": false, "default": "'[]'::jsonb" },
    { "name": "embedding",   "type": "vector",      "nullable": true,
      "embed_source": ["domain", "description", "aliases"],
      "comment": "pgvector 1024-dim embedding computed by SERV from domain+description+aliases. NULL until backfill-embeddings.mjs runs on existing rows." },
    { "name": "created_at",  "type": "timestamptz", "nullable": false, "default": "now()" },
    { "name": "updated_at",  "type": "timestamptz", "nullable": false, "default": "now()" }
  ],
  "foreign_keys": [],
  "constraints": [
    { "type": "unique", "name": "uq_domainhelp_domain", "columns": ["domain"] }
  ],
  "triggers": [
    { "name": "trg_pgc_domainhelp_updated_at", "timing": "BEFORE UPDATE", "function": "set_updated_at()" }
  ]
}
